# src 全量代码审查报告

> 日期:2026-06-10 · 依据:`docs/specs/2026-06-10-src-code-review-design.md` · 状态:进行中
> 基线:`docs/src-code-review` @ `9c05acf`,工作区干净

## 1. 执行摘要

(Task 15 定稿时填写:整体结论、四维度概评、最值得做的 3 件事)

## 2. 发现列表

### 2.1 台账(深读期增量追加,复核后移入 2.2)

> **全部复核完毕(Task 14,2026-06-11)** —— 141 行已全数移入 §2.2:Medium+ 37 行逐条对源复核(确认 35 / 降级 0 / 移除 0,其中 2 对〔4 行〕按行内预定指令合并为 2 行:按钮族 + 表单族触控 → [M-7],Segmented↔Tabs 骨架 + TabBar 第三实例 → [M-22]),Low 104 行按批次双层审查结论补置信度。合计 §2.2 = 139 行(141 − 合并净减 2),数量守恒;复核期另修正两处行内子断言([M-19] children 重渲染表述、[M-7] 的 Input/types.ts:22 失实枚举,均已在行内标注)。

### 2.2 定稿发现(Task 14/15 按严重度分组填入)

(行格式沿用台账六列,「位置」列前置稳定锚名 **[H-x] / [M-x] / [L-x]**(Critical 记 [C-x],本次为 0),全报告交叉引用一律指锚名;节内按维度(质量 → 最佳实现 → 重复 → 解耦)、再按模块(scripts → theme → utils → icons → components/ui → components/business → docs/meta)排序,模块内沿台账录入序。)

#### Critical

(0 行。台账升级条款 —— 如 [H-2] 之于「消费端实际渲染受影响图标」、[M-9] 之于「消费仓出现非受控用法」、[M-3] 之于「消费 app 放开横屏/iPad」、[L-60] 之于「web 多屏差异配置」—— 均以消费端核实为触发条件,本仓审查范围内无 Critical 坐实。)

#### High

| 位置 | 维度 | 严重度 | 置信度 | 证据摘要 | 建议 |
| --- | --- | --- | --- | --- | --- |
| **[H-1]** `scripts/build-icons.js:29、33-35` | 质量 | High | 高(三审直读 :17/:29 —— 元素白名单与双引号限定直接可见;边界 fixture 系批次沙箱实证) | 不支持内容一律静默丢弃,exit 0 零告警,空图标照常进类型闭集 —— /tmp 沙箱(脚本 + 全量 svg 复制)喂 6 个边界 fixture 实证:(a)纯 polyline/polygon 文件 → `elements: []` 仍写入 ICONS 且名字进 `IconName` union,运行时渲染空白但类型完全合法;(b)path 与 `<line>` 混合 → line 静默丢、图标缺笔画(头注释与 CLAUDE.md 只声明 polyline/polygon 需转 path,line/ellipse 同样不支持但未提);(c)单引号属性 `d='…'` → `attr()`(:17)只认双引号 → 路径整体丢失、图标全空;(d)`<g transform>` → 包装层被忽略、子 path 按未平移坐标抽出,几何静默错位。契约已承认元素白名单限制,但违反时的失败模式是静默产坏数据而非 fail-fast,「扔 svg → 跑脚本 → lint → typecheck」全流程不会暴露,坏图标直达运行时 | 脚本收尾加零依赖校验:全文扫 polyline/polygon/line/ellipse/g/use/defs 标签与单引号属性,外加 `elements.length === 0` 检查,命中即列出文件名 `process.exit(1)`,把契约从「注释提醒」升级为「违反即红」。注意上述检查存在已实证盲区:元素 regex `[^/>]*`(:29)使**受支持元素**在任一属性值含 `/` 时整元素静默丢(如 `class="a/b"`),且不触发前述任何检查——须再加「源内 path、rect、circle 开标签计数 == 抽出元素数」交叉校验兜底 |
| **[H-2]** `scripts/build-icons.js:21-22、33-56`(↔ `src/icons/svg/` 13 文件、`src/components/ui/Icon/Icon.tsx:47、54`) | 质量 | High | 高(三审复核:stop.svg 源 → data.ts:2052-2063 生成物 → Icon.tsx:54 渲染链逐环直读;10 处 stroke="none" + 2 处 opacity 文件清单 grep 复现) | 属性白名单(几何 + `fill="currentColor"`)之外静默丢弃,已伤及在售图标,13/113 个的 RN 端渲染偏离 svg 源设计:(a)`stop.svg` 是 filled 风格(根 `<svg fill="currentColor">` 无描边,rect 继承 fill 应为实心方块),生成物(data.ts:2052-2063)rect 无 fill → 运行时 Icon.tsx:54 `el.fill ?? 'none'` 加 :47 根 Svg stroke 被子元素继承,实心方块渲染成 1.75 描边空心方块,用户可见失真(整个 Icon 组件的描边渲染本就依赖这条继承链,113 个描边图标能正常显示即其反证);(b)元素级 `stroke="none"` 共 10 文件(calculator/scanner/shutter/shutter-recording/storefront-ai/storefront-tag/dashboard-star/dashboard-megaphone/error-alert/app-fallback)被丢 → 设计为纯 fill 的圆点运行时继承根 stroke,r=.9 的点视觉直径近翻倍;(c)`opacity=".55"`(aspect-16-9/aspect-4-3 的格线笔画)被丢 → 按全不透明渲染。svg 源在浏览器预览全部正常,偏差只在 RN 端出现且无人告警(文档站预览的也是 svg 源,同样暴露不了)。升级条款:一经证实任一受影响 name 被消费端实际渲染,即按「用户可见 bug」升 Critical(stop/shutter-recording 属录音类 UI,概率最高)——在消费仓 grep 这 13 个 name 一次即可定级 | `IconElement` 增补 `opacity?` 与 `stroke?: 'none'` 并在脚本抽取、Icon.tsx 渲染端透传;`stop.svg` 改描边风格或给 rect 显式 `fill="currentColor"`;最低限度先让脚本对已识别元素上的未抽取属性(opacity/stroke/transform)告警退出,与 [H-1] fail-fast 同批实现 |
| **[H-3]** `src/components/ui/Spinner/Spinner.tsx:36-43(↔ Spinner.web.tsx:30-31、60-66)` | 质量 | High | 高(三审复核:reanimated 4.4 源三段(System 默认 / withTiming 跳端值 / withRepeat 单轮终止)与双端组件代码直读) | 系统开启「减弱动态效果」时两端行为相反且 native 端旋转冻结:reanimated 4.4 动画默认 `ReduceMotion.System`(`react-native-reanimated@4.4.0` animation/util.ts:150-155,默认读系统开关),开启后 `withTiming` 直接跳到端值(util.ts:506-515)、`withRepeat(-1)` 一轮即终止(repeat.ts:68-72)→ native Spinner 永久静止在 360deg(视觉同 0deg),呈一枚顶部异色的静止圆环,加载语义丢失、形似卡死;web 版 CSS `@keyframes` 无 `prefers-reduced-motion` 处理(:30-31、:64)→ 照常旋转。加载指示属「essential motion」,惯例是豁免减弱或换静态可辨识形态;src 全仓 grep 无任何 reduceMotion 决策痕迹 —— 现状是 reanimated 默认值的意外副作用而非设计决策。定 High 依据(质量复审改判):由终端用户系统开关**当下即触发**、无需消费端任何解锁条件(对照 [M-3] 的 Medium+升级条款结构);reduce-motion 用户看到的静止圆环与空闲/完成不可区分,组件唯一职能 100% 丧失且形似卡死——Apple 自家 UIActivityIndicatorView 在该设置下照转,W3C 对 prefers-reduced-motion 的指引是「换更克制形态」而非移除状态信号,业界惯例把加载指示归为 essential motion;同机制连带 native Skeleton/PulseDot 一并冻结(见 [M-19] (c)),加载三件套同场失效,属页面级失效。unmount 清理本身合格(:42 `cancelAnimation`)。 | native 给 withTiming/withRepeat 显式传 `reduceMotion: ReduceMotion.Never` 并注明 why(spinner 为必要动效);若设计选择尊重减弱,则两端统一为静态 reduced 形态(web 补 @media 查询)—— 总之两端语义对齐 |
| **[H-4]** `src/components/ui/Card/styles.ts:9 + Card.tsx:25-28(↔ react-native@0.85.3 RCTViewComponentView.mm:256-271、325、620、836-843)` | 质量 | High | 高(三审复核:Fabric 三段源码(legacy shadow 直写 layer / overflow→clipsToBounds / 豁免名单无 legacy shadow)与组件同层叠加直读;真机仅证伪通道,判定规则行内已载) | Card 默认 variant 的卡片阴影在 iOS 被自身 `overflow: 'hidden'` 裁掉:base 样式的 overflow hidden 与 legacy `shadow*`(useShadow().card 五件套)同设在一个 View 上;Fabric 源码核验 —— legacy shadow 直写 `self.layer.shadow*`(:256-271),overflow hidden 映射 `clipsToBounds`(:325),而保护「overflow ink」的 custom container 机制(:620)仅豁免新 `boxShadow`、非零 border、outline 三者(`styleWouldClipOverflowInk` :836-843,legacy shadow 不在豁免名单)→ 默认无边框的 Card 命中同层 `masksToBounds=YES` 裁剪,iOS 亮色下标准卡片阴影静默消失。Android elevation 由父级按 outline 绘制不受自裁剪影响照常;web 端 RNW 把 shadow* 映射 CSS box-shadow、元素自身 overflow hidden 不裁自己的 box-shadow → 文档站 demo(card.mdx:13、21 自述「唯一的卡片阴影」「default · 带阴影」)看起来全对,缺陷只在 iOS 真机暴露,与 [M-11]「web 预览掩盖 native 缺陷」同模式。暗色 shadow 本就置零不受波及;振幅说明:shadowOpacity 0.08 较轻、丢失不易目测,正因如此可长期无人报。 | 经典双层修复:阴影留外层包装 View、`overflow: 'hidden'` 收进内层(或仅 bare/贴边媒体场景才开 overflow);长线评估迁 RN 新 `boxShadow` prop(Fabric 对其有 overflow-ink 保护通道);真机复核须**default vs plain 并排对照**(亮色)而非单渲染 default(0.08 振幅单看易掉进「不易目测」陷阱),判定规则:机制已源码闭环,真机仅作证伪通道——并排无差异 → 维持 High,意外现影 → 机制误判降级。与 [L-39](Card types 失实)双向互链:types (a)(b)(c) 描绘的「default 本应带边框」旧设计若复原(非零 border),恰好触发 styleWouldClipOverflowInk 豁免通道、阴影顺带解裁——既是回归叙事也是候选修复路径,两行须同批决断 |
| **[H-5]** `src/components/ui/Confirm/confirm.ts:35-52(↔ ConfirmHost.tsx:22-28)` | 质量 | High | 高(三审直读:cleanup 只删订阅、resolve 闭包独占解锁、零 Host 先占后空发射,机制全链在源) | confirm() 的 Promise 悬挂 + `_activeEntry` 永久锁死。主案(契约内可达):(a)Host 带着打开的对话框 unmount —— Host 正确挂根部仍会因上层 remount 卸载(error boundary 重置、根级 re-key 切语言/主题),订阅清理(ConfirmHost:25-27)只 delete sub,`entry.resolve` 永不被调,对话框随 Modal 卸载消失、调用方 await 续体永久挂起 + `_activeEntry` 锁死,事后重挂新 Host 也无法恢复(模块级状态仅 resolve 闭包可清);(b)冷启动首帧时序窗口 —— passive effect 按树序执行,本仓推荐挂法(UNIF-DESIGN.md:539,Host 殿后)下首屏组件 mount effect 里调 confirm()(恢复草稿类场景)必然先于 Host 订阅,无需「漏挂」。子情形:Host 完全未挂载即调用(契约外但零信号)—— `_subs` 为空,:51 forEach 无人接收,Promise 永不 settle 而 :50 已占 `_activeEntry` → 此后一切 confirm() 走 :35 重入分支永远 resolve(false)+warn,文案只说「已有对话框在显示」,误导排查方向。定 High 支柱是不可恢复性(API 唯一职能永久丧失 + 吞掉调用方控制流),非触发频率。对照:toast 同场景仅静默丢一条、无状态锁。 | Host unmount cleanup 时若仍有 entry 先 `entry.resolve(false)`;confirm() 发射后检 `_subs.size === 0` → dev warn + 立即 resolve(false)(并不占 `_activeEntry`) |
| **[H-6]** `src/components/ui/Toast/ToastHost.web.tsx:88(↔ styles.ts:8-18、ToastHost.tsx:78-83)` | 质量 | High | 高(三审直读:styles 注释契约、web :88 仅挂 host、全文件零 entry.position 引用三点复现) | web 版完全未实现定位注入:styles.ts:9 注释自言「定位(top/bottom/center)由 ToastHost 按 entry.position 注入,这里只留通用」,native 版按约定注入 top/bottom + insets(:78-83),web 渲染处(:88)却只挂 styles.host(absolute + left/right,无 top/bottom)→ 垂直落在 CSS 静态位置,随 Host 在根节点中的挂载次序漂移(挂内容之后 = 视口底缘之外,可能整条不可见;挂最前 = 顶边无间距),且全文件零处引用 entry.position(grep 坐实)—— `toast({ position: 'top'/'center' })` 在 web 被静默忽略、滑入方向恒 +8px;头注释「行为复刻 native 版」逐条列了 opacity/translateY/duration 却不含定位,契约缺口未声明。亦是组件级平台分叉行为漂移的现实佐证(↔ [L-95])。 | web 同样按 entry.position 计算 posStyle(bottom/top 定值 + env(safe-area-inset-*) 或 RNW 版 safe-area-context),并把定位纳入头注释的「行为复刻」清单;最低限度先钉死 bottom 定位 |

#### Medium

| 位置 | 维度 | 严重度 | 置信度 | 证据摘要 | 建议 |
| --- | --- | --- | --- | --- | --- |
| **[M-1]** `scripts/build-icons.js:16-19` | 质量 | Medium | 高(三审直读 :17 regex 无左边界;rx 前置碰撞系批次沙箱实证) | `attr()` 属性 regex 无左边界,属性名前缀碰撞时取错值:沙箱实证 `<rect rx="3" x="5" y="6" …>`(rx 前置)→ 生成 `x: 3`(应为 5),静默产出错误几何;同类碰撞还有 y↔ry、d↔id、r↔filter。当前 113 个源全部 x/y 先于 rx,且沙箱再生成与 committed data.ts 语义级 113/113 一致(与 [L-1] 的字节级核对互证),属潜伏;但 SVGO `sortAttrs` 类按字母序排属性的导出链(height、rx、width、x、y)会让后续新增图标必然触发 | `attr()` 的 regex 给属性名加左边界(行首或空白锚定),并把 rx 前置样例作为脚本自测样本 |
| **[M-2]** `scripts/build-icons.js:24-27`(↔ `Icon.tsx:45、49-50`) | 质量 | Medium | 高(三审直读:脚本零校验 + Icon.tsx:45/49-50 硬编码) | CLAUDE.md 规定的 SVG 规范(viewBox 0 0 24 24、round caps)脚本零校验,运行时却写死:Icon.tsx:45 硬编码 `viewBox="0 0 24 24"`、:49-50 硬编码 round linecap/linejoin。沙箱喂 `viewBox="0 0 16 16"` 文件照常生成、exit 0 → 运行时按 24 网格渲染,图标缩至 2/3 且偏左上,静默失真。当前 113 源 viewBox 全合规、root stroke-width 仅 1.75/2 两档(脚本有抽取,:26-27),规范纯靠人工纪律;另 stroke-width 只读根 `<svg>` 标签,元素级覆盖会被丢(现源无此用法) | 脚本断言 viewBox 恒为 `0 0 24 24`,对非 round caps 与元素级 stroke-width 告警;与 [H-1]/[H-2] 的 fail-fast 校验同批实现 |
| **[M-3]** `src/theme/scale.ts:10-16` | 质量 | Medium | 高(三审直读 scale.ts,机制直接可见;触发面在消费端,升级条款行内已载) | 模块加载时一次性取 `Dimensions.get('window').width` 定死 ratio,无短边守卫亦无竖屏假设注释:横屏启动时按长边算(iPhone 17 Pro 874/402≈2.17,全部 `r()` 尺寸约翻倍、`rf()` 字号 +35%),旋转后也不重算;`rf` 注释自引的 react-native-size-matters 正是用 min(width, height) 短边防此例。触发面主要在消费 app:一旦允许横屏/iPad 即用户可见布局劣化(届时应升 High);仓内 example 的方向配置(Android 无 `screenOrientation` 锁、iOS iPad 键放开横屏)仅佐证仓内无竖屏锁定惯例——example 现为未渲染本库的死脚手架,证据成色有限。iPad Split View 启动(window 宽可小于 402)同属此启动抽奖,`min(w,h)` 修法一并覆盖。`width 为 0 时回退 DESIGN_WIDTH` 的防御已具备,不在此列。 | ratio 分子改 `Math.min(width, height)`(竖屏行为不变,一行同时收口横屏启动与旋转漂移);若刻意只支持竖屏 phone,补 why 注释声明边界 |
| **[M-4]** `__tests__/utils/logger.test.ts:64-87`(对照 `src/utils/logger/logger.ts:12-14、24、54-56`) | 质量 | Medium | 高(三审直读测试与实现:用例顺序与分支缺口直接可见) | logger 两个契约分支无用例钉死:(a)`setLogLevel` 追溯生效 —— `emit` 在调用时实时读 `state.level`(logger.ts:24),但唯一 setLogLevel 用例是先设级后 `createLogger`(:77-78),若实现回归成「createLogger 时捕获 level」套件仍全绿;真实消费序列恰相反 —— src 内 7 处模块级 `const log = createLogger(…)`(Icon/Grid/Radio/Stepper/Spinner×2/confirm)在 import 时即已创建,消费 app 启动后才调 setLogLevel,该回归会让 setLogLevel 对库内全部日志静默失效。对照:transports 的同类追溯性已被 removeTransport 用例钉死(:107-109,先创建 logger 后移除)。(b)默认级别仅覆盖 `__DEV__=true → debug`(:64-73,注释自言依赖 jest preset 默认值),生产分支 `warn` 与 `typeof __DEV__ === 'undefined'` 兜底(logger.ts:12-14)零用例。其余契约(transport 吞错隔离 :112-130、同 id 去重 :89-110、scope 前缀、级别门控)均已有用例。 | 补两类用例:① 先 createLogger 再 setLogLevel,断言已存在 logger 实例即时受控;② resetModules 后置 `globalThis.__DEV__ = false`(及 delete 之)再 require,断言 `getLogLevel()` 为 `warn` |
| **[M-5]** `src/components/ui/Logo/Logo.tsx:25(对照 Thumbnail/Thumbnail.tsx:36-37)` | 质量 | Medium | 高(三审复核:Image.ios.js accessible 推导与组件代码直读) | 头注释自述「提供尺寸 / 圆角 / a11y 标准化包装」(:6),但只设 `accessibilityLabel` 不设 `accessible`:RN 0.85 的 Image 在 iOS 上 `accessible = ariaHidden !== true && (alt !== undefined ? true : props.accessible)`(react-native@0.85.3 Libraries/Image/Image.ios.js:170-171),不传 alt/accessible 即不是 accessibility element → VoiceOver 不聚焦不朗读,组件唯一的 a11y 功能在 iOS 实际不生效;同批 Thumbnail 已示范正确姿势 `accessible={!!accessibilityLabel}`(Thumbnail.tsx:37,types.ts:23 还注明「不传 RN Image 默认 hidden」)。另默认 `label = 'Logo'`(:18)即便生效也是无信息量文案。 | 对齐 Thumbnail 模式单一方案:label 转可选、无默认文案、`accessible={!!label}`——把装饰/信息的决定权交给 caller(不传即维持 RN Image 默认 hidden,等价于装饰用法);**不要**走「补 accessible={true} + 默认 'Logo'」,那会把 SR 静默变成逐处朗读 'Logo' 的噪音并关死装饰用法。Android 按 RN 通则同样不可聚焦(行内仅 iOS 实证),修法相同 |
| **[M-6]** `src/components/ui/BlurLayer/BlurLayer.tsx:31-35` | 质量 | Medium | 高(三审复核:库默认 '#FFFFFF' 与 Swift isReduceTransparencyEnabled 在 node_modules 直读;组件未传 prop 在源) | 与 [H-3] 同家族的第三例「系统 a11y 开关 → 行为分叉」(质量复审补录):iOS 开启「降低透明度」时,`@sbaiahmed1/react-native-blur@4.6.2` 的 BlurView 用默认 `reducedTransparencyFallbackColor = '#FFFFFF'` 整面替换模糊(库源 src/BlurView.tsx 默认参恒随 commonProps 下发;ios/Views/BasicColoredView.swift:15、34-41 在 `UIAccessibility.isReduceTransparencyEnabled` 时渲染纯色矩形),BlurLayer 未传该 prop → 暗色 scheme 下 sheet backdrop / 玻璃条变不透明刺眼白底;web 端无 prefers-reduced-transparency 处理、照常模糊,两端再次分叉。 | 按 scheme 传暗色友好的 `reducedTransparencyFallbackColor`(如 `c.surface`),一行修复;与 [H-3] 同批做「系统 a11y 开关」专项对齐 |
| **[M-7]** `src/components/ui/Button/ButtonBase.tsx:65-95、Chip/Chip.tsx:41-50、Stepper/Stepper.tsx:49-92 + styles.ts:59、61、Segmented/Segmented.tsx:32 + styles.ts:15、Checkbox.tsx:30、Radio.tsx:37、Switch.tsx:69、Search.tsx:40、PasswordInput.tsx:39(↔ theme/tokens.ts:93-97、:105-112)` | 质量 | Medium | 高(三审复核:全部高度 / hitSlop 逐值对源,tokens 契约在源) | 全库触控目标系统性低于 `fixed.hitTarget = 44pt`(合并行:原按钮族触控行 + 表单族触控量化行,按预定合并指令)。**按钮族(零 hitSlop)**:sm/md 高度 = control.sm/md = r(28)/r(36)(402pt 基准即 28/36pt,窄屏更小);text variant 高度 undefined(ButtonBase:77),任何 size 命中高都只剩单行文本 ~17-18pt;IconButton square 时 28×28/36×36 双向不足;Chip 可按压而内容高约 2×r(8) padding + xs 行高 ≈34pt;lg=r(44) 在窄屏也缩到 ~41pt(tokens.ts:90-92 注释自言 control.lg 是 scaled、hitTarget 是物理常量「不要合用」)。**小控件(hitSlop 部分补偿后仍欠)**:Radio 4 / Checkbox 4 / Search 6 / PasswordInput 8 / Switch 6 —— 实际命中高 Checkbox ≈ r(20)+8≈28、Radio ≈ r(20)+2·r(6)+8≈40、Switch r(20)+12=32(宽恰 44,按内容宽计,父布局拉伸可放大,见 [L-37])、Search 清除按钮 14+12=26(全场最小;icon 14 系 [M-18] 已记裸值)、PasswordInput eye 约 40×32。**零补偿件**:Stepper 按钮(sm=control.sm、md=r(32))与 Segmented item(高 control.sm=r(28))hitSlop 为零,且父容器(wrap/seg)与子项同高,而 RN 触摸区不越父边界 → 单给 item 加 hitSlop 垂直方向无效,须容器一并让出空间。对照与契约:`fixed.hitTarget = 44` 是仓内自有 token、注释自引 iOS HIG ≥44pt(tokens.ts:105-106),仓内已有消费惯例 —— Tabs/styles.ts:15 `height: fixed.hitTarget`、NavBar/styles.ts:25 `minWidth: fixed.hitTarget`(原按钮族行所引第三例 Input/types.ts:22 经 [L-33] 核实为失实注释引用 —— 实际默认是 control.lg,表单族对 hitTarget 实际消费为零,本合并行枚举已修正);r() 缩放使窄屏数值进一步收缩。影响面 = 全部 sm/md/text Button + IconButton + 可按压 Chip + 表单族小控件;flush Cell 行高同族另记 [L-49]。 | 族级一次决断、分形态落地:① ButtonBase 按 size 推导 hitSlop(contained 形态垂直 `Math.max(0, (fixed.hitTarget - sizing.h) / 2)`,square 水平同补);**text variant 该公式无效** —— 其实际渲染高 ~17-18pt 系 :77 置 height undefined 所致而 sizing.h 仍是 36,需 onLayout 实测或按行高估算另行推导;Chip 同理(修复 [M-8] 命中区后内容高 ~34pt,hitSlop 更必要,两修同批落地);② 小控件按 (44 − 实际高)/2 补垂直 hitSlop;③ Stepper/Segmented 须容器加垂直 padding 或 item 高度直接对齐(Segmented 评估与 Tabs 对齐 44pt);④ Search 清除 / PasswordInput eye 类内嵌微目标若刻意紧凑,在 types 注明边界。Caveat:RN hitSlop 不越父边界,但**同父相邻 sibling 的 slop 重叠不受钳制**(重叠区按渲染逆序裁决),需设 slop 上限或约定相邻间距 ≥2×slop,危险按钮(danger/cancel 相邻)尤甚;刻意紧凑场景在 types 注明边界。 |
| **[M-8]** `src/components/ui/Chip/Chip.tsx:41-50(↔ Chip/styles.ts:16、Chip.tsx:28)` | 质量 | Medium | 高(三审直读::47 style 回调仅 opacity、:28 style 注入内层、alignSelf 只在内层) | 不可见命中区拉伸:可按压分支的外层 Pressable 无任何布局样式(style 回调仅 opacity,:47),列向 flex 父容器(alignItems 默认 stretch)下 Pressable 拉满整行宽,而 `alignSelf: 'flex-start'` 防拉伸保护只设在内层 View(styles.ts:16)→ 视觉胶囊保持 content 宽、命中区却横跨整行,点 chip 右侧空白也触发 onPress;且 `style` prop 注入的是内层 View(:28),caller 无法触达 Pressable 自行修正。行向 wrap 容器(chip 常态用法)横向不触发,但行向容器内存在更高 sibling 时 Pressable **纵向**同样被 cross-axis stretch;列向直排则横向拉满。对照:ButtonBase 把 alignSelf 设在 Pressable 本体(:86),无此问题 | Pressable 补 `alignSelf: 'flex-start'`(与内层同源一行修复);若按 [M-27] 收进 ButtonBase 则顺带消除 |
| **[M-9]** `src/components/ui/TextField/TextFieldBase.tsx:48 ↔ Search/Search.tsx:21、25-34` | 质量 | Medium | 高(三审直读:filled 判定、onSubmit 守卫、spread 顺序全在源;docs 承诺面系批次核证) | 非受控模式成片盲区(defaultValue 在三层类型 TextFieldBaseProps/InputProps/SearchProps 均未 Omit,属类型面放行的合法用法):filled 视觉态只认受控 value(:48 `value != null && String(value).length > 0`)→ 非受控输入有内容失焦后回落 idle 灰底,与受控视觉分叉且零信号;Search 同根三连 —— filled(:21)非受控永 false 使清除按钮永不出现;onSubmitEditing(:32-34)被 `typeof value === 'string'` 守卫,非受控时 onSubmit 永不触发(`e.nativeEvent.text` 现成未用);且 `{...rest}`(:25)展开在 returnKeyType / accessibilityRole / onSubmitEditing 显式 props 之前,caller 传同名 props(类型面放行)被静默覆盖 → return 键回调在「非受控」与「自带 onSubmitEditing」两路全死。PasswordInput 因 types 强制受控(value/onChangeText 必传)不受波及,反证族内对「是否支持非受控」无统一决断。加重证据:website docs(search.mdx:74、input.mdx:113)明文「所有 RN TextInput props 都直接可用」——defaultValue/onSubmitEditing 不止类型面放行,是文档承诺。升级条款:消费仓一经发现非受控用法即升 High | 决断族级边界:支持非受控则 filled 改 onChangeText 镜像内部 state、Search onSubmit 改读 e.nativeEvent.text 并链式调用 caller 的 onSubmitEditing;不支持则**仅 Omit defaultValue 不够**(非受控主形态是「不传 value」——value 在 TextInputProps 本就可选,tsc 实证 `<Search onSubmit={fn} />` 照样编译通过)——须 value/onChangeText 必传(PasswordInput 已示范)+ Omit defaultValue + docs 同步改写「所有 props 可用」承诺 |
| **[M-10]** `src/components/ui/Checkbox/styles.ts:21-25、Radio/styles.ts:31-35、Form/styles.ts:47-51` | 质量 | Medium | 高(三审直读三处 label 样式与 FormRow;RN flexShrink 默认 0 系既定平台事实) | 三处 label Text 同处 flexDirection row 容器且无 flexShrink/flex(RN flexShrink 默认 0,与 web 默认 1 相反):长 label 不换行不截断,直接溢出行容器 —— Checkbox 的 circle 形态自荐「协议同意」场景(Checkbox.tsx:14),恰是长文案高发位;FormRow 虽设 numberOfLines={1}(FormRow.tsx:21)但 Text 宽度从未受约束故截断永不生效,长 label 把 rowControl(flex:1,Form/styles.ts:55-58)挤到 0 宽 → 右侧控件整个不可见。与 Button label 行(Low)的级差分界:后果不同级(此处控件被挤 0 宽=功能丧失,Button 是纯视觉破相)、触发概率不同级(circle 形态自荐的协议同意场景几乎必然长文案,自荐用法即触发) | 三处 label 补 flexShrink: 1 —— FormRow 顺带让 numberOfLines 真正生效;Checkbox/Radio 另需决断长 label 换行还是截断 |
| **[M-11]** `src/components/ui/Form/styles.ts:55-58 ↔ website/docs/components/form.mdx:29-34` | 质量 | Medium | 中(Yoga flex-basis-0 于 auto 宽容器塌缩属经典行为推断,行内自评 85-90%,未真机;非「低」不降档,升 High 以运行时坐实为门) | FormRow + Input(docs 与 Form/types.ts:22 的首选组合)在 native 端疑似宽度塌缩(质量复审补录):rowControl `{ flex: 1, alignItems: 'flex-end' }` 取消 stretch → TextFieldBase 外层 View(无宽度样式)收为内容宽,而 wrap 内 TextInput `flex: 1`(flexBasis 0%)+ minWidth: 0 对 auto 宽容器的尺寸贡献为 0 → wrap 收缩为水平 padding 宽(约 24pt)。docs LiveDemo 跑在 web(CSS 给 input 内在默认宽,恰好掩盖),native 零运行时覆盖(example 死脚手架)。置信度自评 85-90%(Yoga「flex-basis-0 子项在 auto 宽容器中塌缩」经典行为,未真机实测)——**运行时坐实即升 High**。 | 修复前先起 example 渲染 FormRow+Input 一屏定级;修复方向:控件槽给明确宽度策略(child 容器恢复 stretch + 控件 maxWidth,或 FormRow 文档声明 Input 需显式宽度) |
| **[M-12]** `src/components/ui/Avatar/Avatar.tsx:39-51(↔ Avatar/types.ts:18)、DrawerHeader/DrawerHeader.tsx:36-42` | 质量 | Medium | 高(三审复核:Avatar.tsx:49 无 accessible 直读;Fabric/Image 机制同 [M-5] 已核) | [M-5](Logo)同模式再两处,types 承诺的 a11y 在 iOS 不生效:Avatar 容器 View 设 accessibilityLabel 不设 accessible —— Fabric iOS isAccessibilityElement 直接映射 accessible prop([L-29] 已证 RCTViewComponentView.mm:350),普通 View 默认不进 a11y 树 → image 模式(无子 Text)SR 完全静默,types:18「同时作为 a11y label(screen reader 朗读)」承诺落空;letter 模式靠子 Text 恰好读出同字,属巧合兜底而非 label 生效。DrawerHeader 的 Image 同 [M-5] Image 口径(有 label 无 alt/accessible 不进树),name 另有可见 Text 兜底、仅头像本体静默,次要成员。 | Avatar 容器补 `accessible={!!label}`(Thumbnail 正例,与 [M-5] 同方案);DrawerHeader 若按 [M-20] 建议复用 Avatar 则自动收敛 |
| **[M-13]** `src/components/ui/Carousel/Carousel.tsx:81-91(↔ react-native-reanimated-carousel@5.0.0-beta.5 Pagination/Custom/PaginationItem.tsx:79-82、141-147)` | 质量 | Medium | 高(三审复核:库 PaginationItem 默认 label/hint/role 与 onPress 空操作链路在 node_modules 直读;包装层未传 onPress 在源) | 分页点是带英文播报的空操作按钮(质量复审补录,实施深读未开库 Pagination 源):库的每个 dot 渲染为 RN Pressable,自带 `accessibilityRole="button"` + 默认 label「Slide N of M」+ hint「Go to Slide N of M」+ selected state,而包装层不传 `onPress` → 库内 `onPress?.(index)` 恒为空操作;SR 用户聚焦 4×4pt 点、听到可操作提示、双击无任何反应——正是 [L-29] 描述的「承诺了调不动」陷阱,且此处**真实进 a11y 树**([L-29] 因不可达才降 Low,按同一逻辑可达即 Medium);叠加 item fallback 是中文「第 N 项」、dots 是英文,单组件双语播报。showIndicator 默认 true,items>1 即全量命中。次要项:autoPlay 翻页对 SR 零播报。 | 与 [L-85] 捆绑决断(其 forwardRef 论据的最强增量):有库 ref 才能给 dots 接 scrollTo;或传 paginationItemAccessibility 本地化/中和;或对 SR 隐藏 dots、改页变化播报 |
| **[M-14]** `src/components/ui/Toast/ToastHost.tsx:56-61` | 质量 | Medium | 高(三审直读::59 无 id 守卫;在途退场已有 cancel+finished 守护、finish 后队列窗独缺的对照亦在源) | 退场完成回调无条件清 entry 的跨线程竞态:dismiss 定时器触发退场动画,完成回调 `runOnJS(setEntry)(null)`(:59)从 UI 线程异步投递回 JS;若投递到执行之间 JS 正在跑的任务调用了 toast(B)(订阅者 setEntry(B) 先入队/先 commit),排队的 setEntry(null) 随后执行,把刚进场的 B 瞬时清掉(B 的 effect cleanup 连带清自己的 timer 和动画)→ 这条 toast 永不显示且无任何痕迹。窗口 = runOnJS 投递到 JS 任务队列真正执行的间隔——JS 线程繁忙时(导航提交、网络回调风暴,恰是 toast 高发时刻)可达几十至几百 ms,非单纯毫秒级一跳;对照:退场动画进行中来新 toast 的路径已由 cancelAnimation → finished=false 守住(:58-59),唯独 finish 之后这段队列延迟无守护。 | 完成回调改带 id 守卫的函数式更新:effect 闭包里捕获 entry.id,`runOnJS(dismissIfCurrent)(id)` → `setEntry(cur => (cur?.id === id ? null : cur))` |
| **[M-15]** `src/components/ui/Toast/ToastHost.tsx:88-100 + ToastHost.web.tsx:88-102(对照 [L-31],超出部分)` | 质量 | Medium | 高(三审复核:grep 全 src 零 liveRegion/announce 复现;pointerEvents="none" 与自动消失在源) | toast 对 SR 全静默且不可回溯:全 src 零处 announceForAccessibility / accessibilityLiveRegion / aria-live(grep 坐实),toast 又默认 3s 自动消失 + 容器 pointerEvents="none" → SR 用户连「事后遍历发现」的机会都没有(表单 error 族至少还留在树上可达),而 toast 往往是操作的唯一反馈通道(「订单提交成功」「网络异常,请重试」),信息对 SR 用户整体丢失;web 版同样无 role="status"/aria-live。超出表单族之处:transient 浮层的正解是出现时主动 announce,静态挂 liveRegion 属性只够 Android 侧补充,修法不同故单列。注:docs 已显式声明该静默并给逃生口(toast.mdx:105-106「Toast 不会被 screen reader 自动播报…请改用模态对话框或宿主侧自行触发播报」)——但 WCAG 4.1.3 不因声明而豁免,且库自身示例文案(「订单提交成功」「网络异常,请重试」)正是 docs 劝阻的「关键反馈」用法,文档自相矛盾。 | Host 显示 entry 时调 `AccessibilityInfo.announceForAccessibility(message)`(iOS+Android 通用),Android 侧 Text 可加 accessibilityLiveRegion="polite" 兜底,web 容器补 role="status" + aria-live="polite";**连 toast.mdx:105-106 的静默声明一起改写**;与 [L-31] 并入同一 a11y 专项 |
| **[M-16]** `src/components/ui/Confirm/ConfirmHost.tsx:49-58` | 质量 | Medium | 中(RN Pressable 默认 accessible 已源码核实(Pressable.js:252);iOS VoiceOver 对嵌套可达 Pressable 的合并行为未真机 —— 行内已按此自 High 降档,维持 Medium) | 嵌套 Pressable 默认进入 a11y 树的可达性风险:RN Pressable 默认 `accessible: accessible !== false`(node_modules react-native Pressable.js:252),backdrop(:49,有 role/label「关闭」)与 sheet 拦截层(:55,无 label、onPress 空)两层皆成 a11y 元素;iOS 按 RN 文档语义 accessible=true「将子树分组为单一可选元素」→ 对话框 title/message/确认/取消按钮疑似被最外层 backdrop 单元素遮蔽(VoiceOver 只听到「关闭, 按钮」,SR 用户只能取消、无法完成确认),Android 侧 sheet 层亦成无 label 的可聚焦空操作节点;「Pressable 包整块内容导致 VO 合并」是 RN 已知陷阱,未真机实测故降一档。ButtonBase 本体 a11y 合格(role=button + disabled state),可达即正常。 | sheet 拦截层显式 `accessible={false}`(纯事件拦截不应进 a11y 树);backdrop 评估改为 absolute 兄弟层或亦 accessible={false}(Android 返回键/onRequestClose 已是可达的取消路径);真机 VoiceOver/TalkBack 验证后定级 |
| **[M-17]** `src/components/ui/Confirm/ConfirmHost.tsx:41-47、59-86(↔ confirm.ts:45-51)` | 质量 | Medium | 高(症状 (a) 机制直读:关闭即置 null + :59 条件渲染先卸内容;(b) 系未实测的已知风险引用,行内已声明不单列) | Modal 关闭生命周期未建模(无 onDismiss 阶段),两个症状:(a)关闭时 entry 立即置 null → `entry ? <>…</> : null`(:59)先卸载全部内容,animationType="slide" 的 ~300ms 退场滑下的是只剩上下 padding 的空 sheet(高度塌缩 + 内容闪没),每次确认/取消交互可见;(b)resolve 闭包同步清 `_activeEntry`(confirm.ts:46)→ 链式 `await confirm(A)` 后紧接 confirm(B) 的常见序列,微任务续体总是在上一轮 Modal 退场动画期内把同一 Modal 的 visible false→true 快速翻转,RN Modal 快速翻转在 iOS 有「不再呈现/卡死」的历史问题(RN 0.85 新架构未实测,故不单列 High)。 | 保留 lastEntry 渲染至 Modal `onDismiss` 再清(修 a);以 onDismiss 为重入闸门 —— 退场期内新 confirm 入队或延迟发射,而非仅靠 `_activeEntry` 互斥(修 b) |
| **[M-18]** `src/components/ui/Icon/Icon.tsx:13`(↔ `src/theme/tokens.ts:81-88`) | 最佳实现 | Medium | 高(三审复核:11 处裸字面量逐一 grep 复现;tokens 阶梯与注释在源) | size 不入缩放体系:默认 `size = 18` 为裸常量,而 theme 专设图标阶梯 `icon.xs = r(18)` 已存在(tokens.ts:81);ui 内调用两套并存 —— TabBar.tsx:43 用 `icon.sm`、Button.tsx:47/55 用 rf 系 `sizing.fs + 2`、IconButton.tsx:40 用 `sizing.fs + 4`(:10 注释自言与 Button 的 +2 刻意有别;均随设备缩放),但 Search.tsx:35/44、PasswordInput.tsx:31、:43、Checkbox.tsx:51、EntryCard.tsx:25、Empty.tsx:23、Grid.tsx:51、Cell/Cell.tsx:80(`flush ? 18 : 20`)、Cell/Leading.tsx:44、:53 共 11 处裸字面量(14-28)不缩放 → 非 402pt 设备上同屏文本/容器按 r/rf 缩放而这些图标恒定,比例漂移(小屏图标相对偏大);StatusDot.tsx:39 为 prop 派生值不计入。两个加重背景:(a)`tokens.ts:79-80` 把 `icon.*` 自述为「图标盒(IconBox)尺寸阶梯」并强调按语义选不混用——TabBar.tsx:43 拿 icon.sm 当字形尺寸已在踩该边界;(b)`website/docs/components/icons.mdx:44、64` 把裸 px 写进文档(「默认 18」「16–20px 内联」),11 处不是偶发残留而是被文档半制度化的并行约定 | 先决断 `icon.*` 阶梯的语义(字形 or 图标盒),再统一迁移:语义允许则默认值改 `icon.xs`,否则改 `r(18)`;11 处裸字面量同批迁移;**docs(icons.mdx)须同步改写**,否则消费端继续写 px;不要在 Icon 内部统一套 `r()`(TabBar/Button 等已传缩放值,会双重缩放) |
| **[M-19]** `src/components/ui/Pulse/usePulse.web.ts:30-48` | 最佳实现 | Medium | 高(三审直读 hook 与包装组件;「children 子树每帧重渲染」子断言经复核夸大、已于证据内修正,核心成本断言不受影响) | web 端 pulse 用 rAF 每帧 `setOpacity`(:41)驱动 —— 每实例每秒 60-120 次 React 重渲染,web 上 Pulse/PulseDot/Skeleton 全家族走此 hook,骨架屏列表 N 实例即 N×fps 次/秒的主线程渲染(直接消费 hook 的 Skeleton/PulseDot 叶组件与 Pulse 包装层每帧重渲染;Pulse 的 children 元素引用稳定、可获 React 引用相等 bailout,「整棵子树随每帧重渲染」系原表述夸大,终审修正;React 19 自动批处理只合并同帧多实例为一次 pass,不减少每组件渲染次数),恰是 native 版注释「整个动画在 UI 线程跑、不占 JS 桥」(usePulse.ts:15)要避免的形态,而仓内已有零 JS 帧成本的现成先例(Spinner.web.tsx 的 keyframes 注入模式)。连带三笔:(a)头注释 :12-14 自述「setInterval 切换 opacity + 外层 CSS transition」,与实现(rAF 连续插值进 React state)不符;(b)翻转判定 `elapsed + 16 >= duration`(:42)假设 60Hz 帧距,跨过翻转窗的丢帧/后台标签恢复会使 `% duration` 回绕而 direction 不翻 → 波形从三角变锯齿(连续同向)直至偶然命中 16ms 窗;(c)无 prefers-reduced-motion 处理,native 端经 reanimated 默认 System 在系统减弱动效时停在 from(机制同 [H-3])→ 两端 a11y 行为相反;另 native 专门修过的「from 变化首帧闪旧 baseline」(usePulse.ts:26-27)在 web 仍在:state 持旧值且 delay 窗内不刷新(:33-36)。 | 首选 react-native-web 原生 `animationKeyframes` 样式字段(rnw 0.21 编译器支持,website 实装版本已验证):hook 返回 `{ animationKeyframes, animationDuration, animationDelay, animationDirection: 'alternate', animationIterationCount: 'infinite' }`——零 JS 帧、from/to/duration/delay 全保、零注入面、渲染件零改动;**不要**走「按配置生成具名 keyframes 注入 style 标签」(与 Spinner.web.tsx:26-29「严禁拼接 props 进 style 标签」的安全注释正面冲突,且 ref 模式会破坏 usePulse 返回 style 的契约);easing 顺带对齐(native withTiming 默认 `Easing.inOut(quad)`,web 现为线性,波形不同);reduced-motion 用 matchMedia 与 native 对齐 |
| **[M-20]** `src/components/ui/Avatar/Avatar.tsx:20-33 ↔ src/components/ui/DrawerHeader/DrawerHeader.tsx:17-30` | 重复 | Medium | 高(三审直读两文件:state / 守卫 / 回调连同中文注释逐行同) | jscpd #1(14 行/64 token):图片加载失败 fallback 逻辑逐行重复 —— `imageFailed` state + `mountedRef` unmount 守卫 + `handleImageError`(连同一句中文注释一并复制)。同一行为两处独立维护,演进需手工同步(如日后补「source 变更时重置 failed」需改两处,漏一处即行为分叉)。 | 抽共享 hook(如 `useImageFallback`,Avatar 目录内 shared 或 utils),或评估 DrawerHeader 头像块直接复用 `Avatar` 原子;与质量维度联动:`mountedRef` 守卫所防的 unmount-setState 告警在 React 18+ 已移除,若深读判该守卫为死重,正确去重是删除而非抽 hook |
| **[M-21]** `src/components/ui/Grid/styles.ts:72-88 ↔ src/components/ui/TabBar/styles.ts:27-43` | 重复 | Medium | 高(三审直读:字段级一致仅 top 偏移异) | jscpd #3(17 行/83 token,本次唯一 typescript 克隆):`badge` + `badgeText` 红色计数角标样式块逐字段一致(`minWidth`/`height` 均 r(16)、`radius.md`、`c.error`/`c.onError`、`t.nano`、`fw.bold`、`lineHeight: 12`),仅 `top` 偏移不同(r(-6) vs r(-3))。同一视觉物两处维护,角标视觉调整需双改。非 sizingFor/paletteFor 约定结构(见 §3.1 分流)。 | 抽共享角标样式工厂(如 ui 内部 `badgeStyles(c)`,`top` 偏移留参数),或评估独立 Badge 原子供 Grid/TabBar 组合 |
| **[M-22]** `src/components/ui/Segmented/Segmented.tsx:27-40 ↔ Tabs/Tabs.tsx:22-35 ↔ TabBar/TabBar.tsx:25-54(延伸:Grid.tsx:39-61、Cell.tsx:88-106、EntryCard.tsx:40-52 的按压 chrome)` | 重复 | Medium | 高(三审直读三文件,骨架要素逐项在源) | tab 渲染骨架同构实为三实例(合并行:原 Segmented↔Tabs 行 + TabBar 第三实例行,按预定合并指令,计数修正吸收入内):jscpd #4(14 行/103 token)报出 Segmented↔Tabs —— `items.map` + gesture-handler `Pressable` + a11y 三件套(`accessibilityRole="tab"`、`accessibilityState`、`accessibilityLabel`)+ `childTestID` + pressed opacity(模式同、值异:0.85 vs 0.7,且 Segmented 项另有 activeBg/activeShadow —— 抽取时须参数化);TabBar 全要素同构(role="tab" + selected state + label + childTestID + pressed 0.7),仅中段插入 icon+badge 使 token 序列打散、jscpd 未报。tab 项的 a11y/testID 行为三处维护、易漂移;仓库已有 ButtonBase 把按钮 chrome 收口的先例,tab-item primitive 提案的成本收益应按 3 实例评估。延伸:按压 chrome(pressed 0.7 + role + label + testID)另再现 Cell、Grid、EntryCard 三处(全 0.7,与 ButtonBase:85 同值异源),连同 Chip([M-27])全仓散落 ≥6 处。 | 评估抽内部 tab-item primitive(render-prop 模式,类 `ButtonBase`)统一三实例的 Pressable/a11y/testID;[M-27](Chip)保持独立(非 tab 形态),但「pressed 0.7 抽共享常量与 ButtonBase 同源」为多行共指的同一修复落点;若修复期判原子独立性优先,则记入 §4 豁免(候选已列 §4.2;按本报告定义不属约定性同形)。容器 tablist 缺失属 a11y 维度另记 [L-34]/[L-48];共享类型上提见 [L-100]。 |
| **[M-23]** `src/components/ui/BlurLayer/BlurLayer.tsx:3-21 ↔ src/components/ui/BlurLayer/BlurLayer.web.tsx:10-22` | 重复 | Medium | 高(三审直读两端:样板段同构、分叉仅渲染体) | jscpd #2(19 行/63 token):平台兄弟文件间重复 —— import 段 + 函数签名解构 + `useColors`/`useTheme` 开场两端逐行同构,克隆体以声明性样板为主,真正分叉只有 BlurView 与 View 降级渲染。平台分叉本身是仓库约定。 | 倾向豁免(样板占比高、抽取收益低);若两端共享推导(blurType/tint 计算)后续增长,再抽 `shared.ts` |
| **[M-24]** `src/components/ui/Spinner/Spinner.tsx:24-36 ↔ src/components/ui/Spinner/Spinner.web.tsx:46-60` | 重复 | Medium | 高(三审直读:防御分支两端 :26-34 / :48-56 逐行同) | jscpd #5(13 行/110 token,单笔最大 token 克隆):入参防御逻辑在 native/web 双实现逐行重复 —— `size`/`thickness` 的 `Number.isFinite` 校验、两条 `log.warn` 中文文案、`safeSize`/`safeThickness` 钳制。属行为性逻辑而非样板:钳制规则或文案改动需两端同步,漂移即两端运行时行为不一致。 | 抽 `Spinner/shared.ts` 导出 `sanitizeSpinnerProps(size, thickness)`(校验 + warn + 钳制一体),两端消费,渲染保持平台分叉 |
| **[M-25]** `src/components/ui/Switch/Switch.tsx:11-38、:63-76 ↔ src/components/ui/Switch/Switch.web.tsx:3-35、:49-62`(jscpd #6/#7 合并) | 重复 | Medium | 高(三审直读:派生常量与 Pressable 外壳两端逐行同) | 2 笔克隆共 42 行/171 token,文件对累计最大:(a)动画端点常量 `THUMB_OFF_X`/`THUMB_ON_X` 的 `r()` 推导连同推导注释整段两端复制,而其依赖 `INSET`/`TRACK_W`/`THUMB` 本就由 `styles.ts` 导出,派生常量却未收口;(b)`Pressable` 外壳(onPress 翻转、disabled、hitSlop 6、a11y switch role/state、testID)两端逐行一致。 | (a)`THUMB_OFF_X`/`THUMB_ON_X` 移入 `styles.ts` 与 INSET 等同源导出;(b)评估抽共享 Pressable 外壳,两端只分叉 track/thumb 动画实现 |
| **[M-26]** `src/components/ui/Toast/ToastHost.tsx:11-28 ↔ src/components/ui/Toast/ToastHost.web.tsx:3-23` | 重复 | Medium | 高(三审直读:开场段与订阅 effect 两端同构、native 多 clearTimeout 一行) | jscpd #8(18 行/71 token):host 开场段重复 —— theme/styles/`_subs`/types import + 函数签名 + `useColors`/`useThemedStyles`/state hooks。另经核读,克隆区下方紧邻的 `_subs` 订阅 effect(native :34-43 ↔ web :29-37)亦两端同构(仅 native 多 clearTimeout 一行——系刻意架构分歧:web 端 timer 清理移入 entry effect cleanup,抽共享 hook 须把 timer 处理留在平台侧或暴露回调),jscpd 因 token 数不足未报。 | 评估把订阅与 entry state 抽共享 hook(如 `useToastEntry`),两端只分叉动画渲染;若判平台分叉约定豁免,修复期决断(候选已列 §4.2) |
| **[M-27]** `src/components/ui/Chip/Chip.tsx:39-52(↔ Button/ButtonBase.tsx:65-102)` | 重复 | Medium | 高(三审直读 Chip 全文,chrome 要素与重叠面直接可见;git 时序系批次考证) | Chip 可按压分支在 ButtonBase 外自建按压 chrome:gesture-handler Pressable 外壳 + pressed opacity 0.7(与 ButtonBase.tsx:85 同值异源)+ `accessibilityRole="button"` + accessibilityState + label/testID 全套 —— 按压 chrome 散落的第三处(另见 [M-22])。定性(经 git 时序考证):**落在契约意图内、字面外,非违约**——CLAUDE.md 契约管「新的按钮型组件」,而 Chip 与 ButtonBase 同入拆库迁移提交 `b090723`(18:16),契约于同日稍后 `3cf554f`(20:03)入仓,Chip 处于契约盲区;且两者真实重叠很薄(Pressable 壳 + pressed + a11y ≈10 行),ButtonBase 的重头机器(sizingFor 定高/variant palette/loading)对内容驱动高度、pill、selected 双态的 Chip 全不适用,自建系当时合理决策。真问题是前瞻性缺口:契约未声明 selected-toggle 形态归属,pressed 0.7 与 a11y 惯例无共享源,第四个类似组件出现时将再散落一份 | 扩 ButtonBase(accessibilityState merge + 可选非交互形态)后 Chip 接入;或修复期判「selected-toggle 语义自成一族」记 §4 豁免(候选已列 §4.2)—— 届时至少把 pressed 0.7 抽共享常量与 ButtonBase 同源,并与 [M-22] 的 tab-item primitive 决策同批定 |
| **[M-28]** `src/theme/colors.ts:92-98、184-188、195-196(对照 palettes.ts:1-15)` | 解耦 | Medium | 高(三审直读 colors/palettes;同源 git 史系批次考证) | 渐变序列驻留 colors.ts,与 palettes.ts 自述职责冲突:`heroGradient0/1/2` 是 3 stop 渐变序列硬塞进 role-based ColorTokens —— palettes.ts 头注释明言该形态是反模式(「硬塞 4 个 role 进 ColorTokens 语义模糊,改放这里」),CLAUDE.md 同口径「新加 palette 走这里,不要往 colors.ts 塞」;`avatarGradient` 渐变数组(:195-196)亦驻 colors.ts。git 核实两者与 palettes.ts(含该头注释)同源于 phase 1 提交 `7dd4231`(并存而非先后遗留)→ 仓内并行两套渐变安置惯例,新增渐变时先例互相矛盾。 | 首选在 colors.ts 两处注明「历史驻留,新渐变一律进 palettes.ts」的边界(CLAUDE.md 与 palettes.ts 头注释对「新增」的指向本就单义,矛盾只困扰归纳旧代码者);迁入 palettes.ts 降为可选项——heroGradient 驻 ColorTokens 享受随主题自动切换,迁成 palette 形态后消费方须手动 `scheme === 'dark'` 分支,除 breaking 外还是人体工学回退 |
| **[M-29]** `src/components/index.ts:1-2`(整文件) | 解耦 | Medium | 高(§3.2 证据链 + 本轮 grep 复核零引用) | madge --orphans 报 8 个孤儿,7 个经排除流程判误报(根入口 1 + `*.web.*` 平台变体 6),本文件是唯一真孤儿(疑似死代码):src 内零 import —— 根 barrel `src/index.tsx:9-10` 直接 re-export `./components/ui` 与 `./components/business`,绕过本文件;包外亦零引用 —— `package.json#exports` 无 `./components` 子路径,example/website 全仓 grep 深路径 0 命中(example 全部从包根 import),jest/babel 配置无模块映射。git 史:`b090723`(phase 2+3 迁移)引入后零改动,且全历史 `src/index.tsx` 从未 import 过 `./components` → 自引入起即未被使用。完整证据链见 §3.2。 | 删除 `src/components/index.ts`(2 行 re-export barrel,无任何契约内可达路径);不建议反向「改走它」——那会为零消费者多加一跳 re-export,且与根 barrel 直连 ui/business 的现行结构相悖;若日后想要 `components` 聚合入口,再按需重建并让根 barrel 经它 re-export |

#### Low

| 位置 | 维度 | 严重度 | 置信度 | 证据摘要 | 建议 |
| --- | --- | --- | --- | --- | --- |
| **[L-1]** `scripts/build-icons.js:74` | 质量 | Low | 高(字节级复现) | Step 5 对 `src/icons/data.ts` 原始再生成 hash 变化(`8b3275e9…` → `4678ebad…`,diff ±2k 行):`build-icons.js` 输出未经 prettier 格式化(`:74` 单行拼接 `IconName` union),而提交版本按文档流程跑过 `yarn lint --fix`(prettier 拆行)。将再生成产物按仓库 prettier 配置(package.json#prettier)格式化后与 HEAD 字节一致(同为 `8b3275e9…`)→ 内容与 `src/icons/svg/` 同步,差异纯格式、再生成不幂等。任务预设「hash 变化即 High(生成物失同步)」,经上述验证证伪,降级记录;工作区已 `git checkout -- src/icons/data.ts` 还原。 | 让 `scripts/build-icons.js` 直接产出 prettier 格式(或脚本末尾调用仓内 prettier 3.8.1),使再生成幂等,`node scripts/build-icons.js && git diff --exit-code` 即可作 CI 一致性校验 |
| **[L-2]** `src/theme/useTheme.ts:6-14` | 质量 | Low | 高(批次双层审查验真) | Provider 缺失时 `useTheme` 静默回退模块级 `FALLBACK`(light 全套 tokens),无 `__DEV__` 告警亦无 why 注释 —— 漏包 `ThemeProvider` 的接入错误被掩盖(整 app 永远 light、暗色失效且零信号);FALLBACK 为模块级稳定引用,useThemedStyles 缓存契约不受影响。fallback-vs-throw 属 CLAUDE.md 注释标准里「读者会问为什么」的决策但未注明。 | 补一句 why 注释,并在 dev 下一次性 `console.warn` 提示缺 Provider(告警用 `typeof __DEV__ !== 'undefined' && __DEV__` 守卫——裸 `__DEV__` 在非 Metro 的 web 打包器下会 ReferenceError,logger.ts:12 已有此先例——配模块级 flag 保证只警一次;theme 是最底层,不引 utils/logger,维持零仓内依赖) |
| **[L-3]** `src/theme/colors.ts:169、172` | 质量 | Low | 高(批次双层审查验真) | 两处暗色 alpha 与亮色故意不同但缺逐条注释(CLAUDE.md 契约:「很多 alpha 值在亮/暗之间故意不同,colors.ts 有逐条注释」):(a)`glassStatsHighlight` 暗 0.10 无注释,且亮侧注释宣称的「比 glassHighlight 更强」(0.65>0.28)在暗色反转(0.10<0.24);(b)`brandTint12` 暗 0.22 无注释并与 `brandTint14` 暗值(:117,同 0.22)撞值 —— 亮色 10/12/14 三档阶梯(0.10/0.12/0.14)在暗色塌缩为 0.16/0.22/0.22,亮侧「比 brandTint10 多 2% 视觉权重」语义不再成立;刻意合档还是笔误无从判别。 | 两处各补一句暗色 why 注释;`brandTint12` 暗值若为笔误改回阶梯中间档(0.18-0.20),若刻意合档则注明 |
| **[L-4]** `src/theme/colors.ts:51 ↔ :156` | 质量 | Low | 高(批次双层审查验真) | 与 [L-3] 同类的第 3 处暗色 alpha 注释缺口:`scrim` 亮 0.5 → 暗 0.7,两侧均无注释(违 CLAUDE.md「亮/暗 alpha 故意不同有逐条注释」契约)。质量复审全量扫描确认显著缺口共 3 处(另有 brandTint14 等 3 处属块级注释勉强覆盖的边缘,点到为止不入账)。 | 补一句暗色加深的 why 注释(如「暗色环境底图对比度低,scrim 需更重才能压住」) |
| **[L-5]** `src/theme/shadow.ts:109-117、120-125` | 质量 | Low | 高(批次双层审查验真) | ShadowTokens 类型纪律与 ColorTokens 不对称:顶层 key 经同态映射保留 readonly,但值类型嵌套字段可合法赋值(tsc 实测:`t.card = {...}` 报 2540,`t.card.shadowOpacity = 3` 通过);叠加 `darkZero` 浅展开,暗色 9 个 token(subtle/card/brand 系/none)共享同一 `shadowOffset` 对象引用——一次类型合法的 `shadow.card.shadowOffset.height = 5` 会静默串改 9 个 token。ColorTokens 因值为 string 经同态映射天然全量只读,无此问题。 | 值类型嵌套字段加 `readonly`;`darkZero` 改为每 token 独立展开 offset 对象(或冻结) |
| **[L-6]** `src/theme/blur.ts:4-9 ↔ src/theme/colors.ts:60、64、163、165` | 质量 | Low | 高(批次双层审查验真) | 注释与档位失配:colors.ts 四处注释宣称 glassSurface/tabBarGlassTint 的 alpha 按「blurAmount=25 / 15」标定,但 blur.ts 仅 10/40 两档、仓内无任何 15/25 用法(grep 0 命中,TabBar 亦不使用 BlurLayer),且文档站 blur-layer.mdx:88 明文禁止 hardcode blurAmount——注释所述设计组合无法用公共 API 复现:注释过时或档位缺口,二者必居其一。 | 对照设计稿二选一:更新四处注释为现档(10/40)标定值,或在 blur.ts 补 15/25 档并文档化 |
| **[L-7]** `src/utils/logger/logger.ts:32-38、62-69` | 质量 | Low | 高(批次双层审查验真) | emit 迭代期间的重入安全是隐式的:`for...of` 直接迭代 `state.transports`(:32),而数组只经 removeTransport 的 `filter` 整体替换引用(:68;addTransport 先 remove 再 push,:63-64),故 transport 在自身 `log()` 内增删 transport 不会破坏在途迭代 —— 现状语义正确,但安全性悬于「filter 返新数组、绝不原地变更」这一未注释实现细节,亦无用例钉死;若日后把 removeTransport 改成惯常的 in-place `splice`,emit 中途自移除会跳过后续 transport,套件不红。 | 给 add/removeTransport 补一句「数组只整体替换不原地变更,保证 emit 重入安全」的 why 注释,或 emit 改迭代浅拷贝 `[...state.transports]`;可选补 transport 在 log 回调内自移除的用例 |
| **[L-8]** `src/utils/testID/testID.ts:8、21` | 质量 | Low | 高(批次双层审查验真) | 头注释代码块所引的被收口旧模式用 `??`(nullish 判定),实现是 truthy 判定(:21 `if (override) return override`),两者在 `override=''` 边界不等价:旧式保留 `''`(显式空 testID),现实现回落拼接返回 `parent-id`。现行为已被测试钉死为刻意(`__tests__/utils/testID.test.ts:31-33`),功能无虞;但注释未标注与所引旧模式的这一差异,bullet 用语「override 命中」也未定义命中即 truthy —— 属仓库注释标准「读者会问为什么」的点,且可能诱导维护者把判定改回 `??`(有用例兜底会红,风险有限)。同一注释块另一处盘点失实(:4-5):所列「7 处 list-like 组件(Tabs/Segmented/TabBar/Grid/Task/Sources/ChainOfThought)」中后三者在本仓零存在(grep 0 命中,系 `b090723` 拆分迁入前消费者仓的状态),本仓读者无法对账。 | 注释补半句:与旧式 `??` 不同,空串 override 视为未提供、回落拼接(用例已钉死);并更新盘点为本仓现状(4 组件已接入 + Radio/Carousel 未接入) |
| **[L-9]** `src/icons/types.ts:12`(↔ `scripts/build-icons.js:44-45`、`Icon.tsx:72`) | 质量 | Low | 高(批次双层审查验真) | 类型-生成-渲染三层契约漂移两处:(a)rect 的 `ry` 类型声明了、Icon.tsx:72 也渲染,但脚本只抽 rx(:44-45)永不产出 → 不可达死支,源 svg 中 ry≠rx 会被静默丢(现源无 ry 用法);(b)类型 `fill?: string` 任意串、Icon.tsx:54 对非 currentColor 值透传,但生成端 `elFill`(:21-22)只可能产 `'currentColor'` → 运行时一般化能力与生成端白名单脱节,读类型会高估支持面;(c)`Icon.tsx:55-77` 的 if 链对 3 成员判别式联合 narrow 正确,但末尾 `return null` 兜底使未来增 kind(如支持 line/ellipse,正是 [H-2] 的修复方向)时渲染端静默渲染空而非编译报错;(d)`ICONS` 的 `Record<IconName, IconDef>` 与 IconDef/IconElement 均无 readonly,消费端可类型合法地原地改 `ICONS.x.elements` 污染全局单例(与 [L-5] 同口径,触发面小)。脚本为零依赖 JS,与 types.ts 形状契约纯靠手工同步,无 ts-check 钉死 | 脚本补抽 ry(一行)或删类型/渲染端的 ry;脚本加 `// @ts-check` + JSDoc `@type` 引用 `IconDef`,让 tsc 看住形状契约;渲染分发改 switch + `never` 穷尽兜底;IconDef/IconElement 加 readonly |
| **[L-10]** `src/components/ui/Icon/Icon.tsx:22-31` | 质量 | Low | 高(批次双层审查验真) | 未知 name 的 fallback 本身合格 —— 不崩溃、按 size 占位保布局、a11y 隐藏、`createLogger('Icon')` 模块级单例符合约定;但 :23 `log.warn` 位于渲染路径且无去重:列表场景同一坏 name 每实例每次渲染都告警,而 logger 生产默认级别恰为 warn,会持续进生产日志 | 模块级 `Set<string>` 记已警 name,同一未知 name 只警一次 |
| **[L-11]** `src/components/ui/Spinner/Spinner.tsx:26-31(↔ Spinner.web.tsx:48-53)` | 质量 | Low | 高(批次双层审查验真) | `log.warn` 位于渲染体内且无去重:坏 props 下每次重渲染重复告警(并发 React / StrictMode 双渲染再翻倍),logger 生产默认级恰为 warn 会持续进生产日志 —— 与 [L-10] 同口径;另防御不对称:size 非有限(NaN/Infinity)会告警并钳 8,thickness 非有限却不告警(:29 条件 `Number.isFinite(thickness) && ≤0` 不含非有限分支)静默 fallback 2,同组件两个数值 prop 两套防御口径。 | 并入 [M-24] 建议的 `sanitizeSpinnerProps`:告警移出纯渲染路径并模块级去重(同 [L-10] 方案),补 thickness 非有限告警分支 |
| **[L-12]** `src/components/ui/StatusDot/types.ts:5` | 质量 | Low | 高(批次双层审查验真) | tone 文档注释「soft:pending = bgCard、active = primary0」指向不存在的 token:两名字全 src 0 命中(grep 验证),系拆库前消费者仓旧 token 名残留;实际实现为 `c.surface` 与 `c.primaryContainer`(styles.ts:31、36),styles.ts:20 同主题注释用的是现名 → 同组件两处注释互相矛盾,误导按 types 提示选 tone 的消费者。 | types.ts:5 改为现 token 名(surface / primaryContainer),与 styles.ts:20 对齐 |
| **[L-13]** `src/components/ui/StatusDot/StatusDot.tsx:44(对照 :39)` | 质量 | Low | 高(批次双层审查验真) | 内容物缩放策略不一致:done 态勾按 `Math.round(size * 0.6)` 随 size 派生(:39,[M-18] 已豁免其裸值),active 态 `<PulseDot />` 全默认恒为 6(:44)—— size=24 时勾放大到 14、活动点仍 6,大尺寸下 active 视觉失衡;加上 borderWidth 走 r(1.5)(styles.ts:12)而 size 默认 16 为裸值,同一组件三种尺寸策略并存。 | active 内点同样按 size 派生(如 `size * 0.375` 取整);borderWidth 与默认 size 的缩放口径并入 [L-74] 统一决断 |
| **[L-14]** `src/components/ui/StatusDot/StatusDot.tsx:23-46` | 质量 | Low | 高(批次双层审查验真) | 状态信息对 SR 完全不可达且无补救入口:子元素 Icon / PulseDot 均自带 SR 隐藏(Icon.tsx:39-40、PulseDot.tsx:31-32),容器 View 无 role/label/state,types.ts 亦未暴露 accessibilityLabel prop —— done/active/pending 语义只剩视觉通道(任务列表中状态常仅由该点表达);同批原子中纯装饰件(Spinner/Skeleton/PulseDot)显式隐藏、信息件(Logo/Thumbnail)暴露 label,StatusDot 作为信息件两头都不沾。 | 暴露 `accessibilityLabel?`(可按 status 派生默认文案),或在 types.ts 注明「状态 a11y 由行级容器负责」的边界约定(若采此约定,同句应一并覆盖 Spinner/Skeleton——loading 语义对 SR 同样零信号且无补救 prop) |
| **[L-15]** `src/components/ui/BlurLayer/BlurLayer.web.tsx:27-30、35(对照 BlurLayer.tsx:26-41)` | 质量 | Low | 高(批次双层审查验真) | 两笔 web-only 视觉魔数无 why 注释:`saturate(180%)`(:27-28)与暗色 `opacity: 0.96`(:30)均无 native 对应、无注释,违反仓库「读者会问为什么」注释标准,两端视觉等效性无从核对;且 `style` 注入点语义不同 —— web 把 caller style 放在 webBlurStyle 之后的同一元素上(:35,可覆盖 tint/backdropFilter),native 放在外层容器(:27,内部 blur/tint 两层 absoluteFill 不可触)→ 传视觉类 style 时两端行为分叉(types.ts:11 注释为「附加样式;默认 absoluteFill,caller 通常不需要传」——隐含布局用途但未明示边界,故降 Low)。`glassTint*`/`sheetBackdrop` token 使用本身合规(暗色 sheetBackdrop 沿用浅 tint 在 colors.ts:157-158 有 why 注释)。 | 两处魔数各补一句 why;types.ts 把「style 仅限布局/定位」写成明确边界,或 web 端把视觉 props 与 native 的注入层级对齐 |
| **[L-16]** `src/components/ui/Skeleton/Skeleton.tsx:16-17`(↔ usePulse.ts:23) | 质量 | Low | 高(批次双层审查验真) | 头注释双重失实:宣称「`usePulse({from:0.5})` 全局共享脉冲(Reanimated 4 worklet 驱动)」——实际 usePulse 每实例独立 `useSharedValue` 独立起步,无任何模块级共享驱动,错峰挂载(FlatList 增量渲染)时各 Skeleton 相位漂移、整屏呼吸不齐,注释承诺的「全局共享」恰是不存在的那个特性;「worklet 驱动」对 web 端(rAF + state)也不成立,而本文件双端单源。 | 注释改为如实描述(每实例独立脉冲);若整屏同步确是设计意图,把 usePulse 升级为模块级共享时钟(顺带消除相位漂移) |
| **[L-17]** `src/components/ui/StatusDot/styles.ts:19` | 质量 | Low | 高(批次双层审查验真) | 注释「签名 (target, context, tokens) 与 Tag/Tool 的 paletteFor 一致」两处失实:`Tool` 全 src 0 命中(拆库前消费者仓残留,与 testID.ts 头注释行同口径);Tag 的 paletteFor 实为两参 `(variant, c)`(Tag/styles.ts:35),无 context 位。同类拆库残留另见 Avatar/styles.ts:39、Tag/styles.ts:34(已核实,见 [L-22]、[L-47])。 | 注释改为与本仓现状一致;全仓一次性 grep 拆库前组件名(Task/Sources/ChainOfThought/Tool)统一清理这类残留 |
| **[L-18]** `src/components/ui/Thumbnail/Thumbnail.tsx:29`(↔ types.ts:10-13) | 质量 | Low | 高(批次双层审查验真) | `uri`/`source` 双缺省时静默 `return null` 零告警,与同批 Spinner 对坏 props「log.warn + 钳制」的防御口径不一致;types.ts 注释写「二选一」但类型未编码该约束,漏传方在编译期(无 XOR union)与运行期(无 warn)均零信号。 | types 用 XOR union 钉死二选一(`{ uri: string; source?: never } 与 { source: …; uri?: never }` 的并),或运行期补一次性 log.warn |
| **[L-19]** `src/components/ui/Button/ButtonBase.tsx:67、85、97-101` | 质量 | Low | 高(批次双层审查验真) | loading 态对 SR 折叠成 disabled:accessibilityState 只有 `disabled: !isInteractive`(:67),无 `busy`(RN accessibilityState 原生支持且两端实播报:Fabric iOS 把 busy 追加进 accessibilityValue——RCTViewComponentView.mm:1487-1488;Android 追加 state_busy_description 进 contentDescription——BaseViewManager.java:438-441)——SR 用户听到「已停用」而非「加载中」;且 RNGH Pressable `accessible` 默认 true(Pressable.tsx:389)使 iOS 扁平化子树,loading 时的 ActivityIndicator 对 SR 完全不可见,证据链闭合;且视觉端 loading 不降透明度(:85 仅 disabled 才 0.5,spinner 表「进行中」),与 SR 端「停用」语义分叉;types.ts:40 自述 loading「自动 disabled」仅指交互拦截,未声明播报语义 | accessibilityState 增 `busy: loading`(disabled 门控保留);website docs 的 button 页顺带注明 loading 的 SR 行为 |
| **[L-20]** `src/components/ui/Button/styles.ts:54-60 ↔ src/components/ui/Tag/styles.ts:51-56` | 质量 | Low | 高(批次双层审查验真) | 穷尽性兜底糊弄(现状 case 齐全——Button 3 size 7 variant、Tag 2 size 6 variant 全覆盖,纯演进风险):Button `sizingFor` 用 if 链 + 尾部 return 兜 md → ButtonSize 增成员时编译零信号、新尺寸静默按 md 渲染;Tag `paletteFor` 用 `default` 兜 neutral → TagVariant 增成员同理;而同两份文件内 Button `paletteFor`(:69-88)与 Tag `sizingFor`(:25-31)恰是 switch 全枚举无 default(noImplicitReturns 钉死穷尽)的正确范本 —— 同文件双纪律并存,「新增 size 在 types.ts 加 union + 这里加 case」(Button styles.ts:34、Tag styles.ts:19)纯靠注释纪律。与 [L-9] (c) 项(渲染分发 return null 兜底)同口径 | 两处改 switch 显式枚举全部成员、不留 default/尾兜底,让 noImplicitReturns 看住 union 演进 |
| **[L-21]** `src/components/ui/Button/Button.tsx:49-53(↔ ButtonBase.tsx:77)` | 质量 | Low | 高(批次双层审查验真) | label 长文本无钳制:Text 无 numberOfLines,而 ButtonBase 高度钉死 `height: sizing.h`(非 minHeight,:77)→ 长 label 或窄容器(尤其 block + flex 布局)换行后总文高超出固定高容器,溢出绘制或被裁(随平台 overflow 行为),按钮破相;同族口径不一:Chip 钳 1 行(Chip.tsx:32),Button 无钳也无 prop(Tag 虽「默认 1 可配」但其可配与定高 22/26 自相冲突——见 [L-24],不作范本) | label Text 加 `numberOfLines={1}`(必要时暴露 prop,且暴露必须连带把高度语义改 minHeight,否则复刻 Tag 同一个坑);与 Chip 对齐 |
| **[L-22]** `src/components/ui/Tag/styles.ts:33-34` | 质量 | Low | 高(批次双层审查验真) | 注释拆库残留确认(StatusDot/styles.ts:19 行预告「同类拆库残留另见 …Tag/styles.ts:34,留待对应批次核对」,本批核实):「签名 (target, context) 与 chat/Tool 的 tintFor / variantFor / labelFor 一致」—— 三个函数名全 src 唯一命中即本注释自身,chat/Tool 组件本仓零存在,本仓读者无从对账;另 `paletteFor` 无显式返回类型(对照 Button 在 types.ts 定义 `ButtonPalette` 并标注返回),`'transparent' as const`(:47)即缺上下文类型时的推断补丁痕迹 | 注释改指本仓现存同形(如 Button/Avatar 的 paletteFor);types.ts 补 TagPalette 类型做返回标注(as const 随之可删);并入 [L-17] 建议的全仓清理——词表须扩为「拆库前组件名(Task/Sources/ChainOfThought/Tool/chat)+ 旧 token 名(bgCard/primary0/fg-1)」双类 |
| **[L-23]** `src/components/ui/Chip/Chip.tsx:10` | 质量 | Low | 高(批次双层审查验真) | 头注释「fg-1 文本」是拆库前旧 token 名残留(全 src 仅此 1 命中,现实现为 `c.foreground`),与 [L-17]/[L-22] 同根模式;同句「白底」对暗色 `c.surface` 亦不准(质量复审补录,本批深读 Chip 时漏掉了自家文件的残留)。 | 注释改现 token 名与双 scheme 准确表述;并入 [L-17] 全仓残留清理 |
| **[L-24]** `src/components/ui/Tag/types.ts:29-30 ↔ Tag/Tag.tsx:19、31` | 质量 | Low | 高(批次双层审查验真) | `numberOfLines` 文档宣称可配(默认 1),但 sizingFor 高度钉死 22/26——传 >1 时第二行必然溢出或被裁,该 prop 在 1 以外的取值全是陷阱且无边界声明;[L-21] 原引它为正面对照已撤销。 | 文档注明「仅在自定义高度场景使用 >1」,或干脆收回该 prop(定高组件多行无意义);若保留则高度语义改 minHeight |
| **[L-25]** `src/components/ui/Button/Button.tsx:10、types.ts:28-29、styles.ts:64` | 质量 | Low | 高(批次双层审查验真) | 注释失实三连:(a)头注释「6 variant」实为 7(types/styles/website docs 三处均 7);(b)label 注释「icon-only 场景请用其他组件(Pressable + Icon)」指引消费者手搓 chrome——IconButton 同提交入仓即官方答案,该指引与 ButtonBase 收口方向相反;(c)styles.ts:64「text / ghost / neutral 共用 transparent 底,差异在 fg」对 ghost↔text 不成立(两 palette 字节级同一,真差异是 ButtonBase:77 的几何撕除)。 | 三处注释如实改写:7 variant、icon-only 指向 IconButton、ghost↔text 差异写明在几何不在 palette |
| **[L-26]** `src/components/ui/Button/ButtonBase.tsx:77-80 ↔ IconButton/types.ts:13` | 质量 | Low | 高(批次双层审查验真) | `square × text` 组合未防御:IconButtonProps 放行全量 ButtonVariant,传 `variant="text"` 时 height 因 isText 置 undefined 而 width=sizing.h → 非方形塌陷盒;且 text 与 ghost palette 字节级同一,该取值在 IconButton 上无任何正当用途。 | IconButton variant 类型收窄为 `Exclude<ButtonVariant, 'text'>`,或 ButtonBase 对 square+text 组合守卫告警 |
| **[L-27]** `src/components/ui/Chip/types.ts:10-11 ↔ Chip.tsx:41-50` | 质量 | Low | 高(批次双层审查验真) | Chip a11y 两缺:(a)types 自荐 trailing 放「小的 × 关闭按钮」,但可按压外壳 RNGH Pressable `accessible` 默认 true → iOS VoiceOver 扁平化子树,× 对 SR 不可达,且外 RNGH 内 RN responder 的嵌套手势行为未定义;(b)`accessibilityState.selected` 只存在于可按压分支,非按压形态的 selected 视觉态对 SR 零通道。 | trailing 含交互元素时外壳设 `accessible={false}` 让子元素独立可达(或文档禁止 trailing 放交互件);非按压分支补 accessibilityState |
| **[L-28]** `src/components/ui/Button/ButtonBase.tsx:97-101` | 质量 | Low | 高(批次双层审查验真) | loading 宽度塌缩:非 block 按钮宽度系内容驱动(alignSelf flex-start、无 minWidth),loading 把 label 整体换成 ~20pt 的 ActivityIndicator → 按钮宽度跳变、邻接布局回流,每次进入 loading 用户可见;与 [L-19] 不同源(一个播报语义、一个几何稳定)。 | loading 时锁定切换前宽度(onLayout 记忆 + minWidth),或 spinner 与 label 叠放(label 透明占位) |
| **[L-29]** `src/components/ui/Stepper/Stepper.tsx:66-75` | 质量 | Low | 高(批次双层审查验真) | 值容器 View 设 accessibilityRole="adjustable" + accessibilityValue{min,max,now},但三件缺失使该承诺在 iOS 落空:(a)未设 accessible —— Fabric iOS 的 isAccessibilityElement 直接映射 accessible prop(react-native@0.85.3 RCTViewComponentView.mm:350),View 默认 false → role/value 根本不进 a11y 树,VoiceOver 只读到内部裸 Text 数字;(b)即便可达,iOS 对 adjustable 的交互契约是聚焦后上下滑调值,而组件无 accessibilityActions/onAccessibilityAction(increment/decrement)→ 系统播报「可调整」但滑动空转;(c)无 accessibilityLabel,播报缺字段名上下文。± 按钮各自 role=button + disabled state + hint(:52-55、:79-82)兜底可用,SR 用户仍可完成操作。定级说明(质量复审 Medium→Low):因 (a) 使 role/value 在 iOS 根本不进 a11y 树,(b) 的「播报可调整但滑动空转」陷阱永不发生——现状是惰性死码 + API 表面承诺失实(与本报告其它「注释/承诺失实」行同 Low 口径),用户侧零阻断;若后续验出 Android 端 adjustable 不受 accessible 门控而实际进树,交互陷阱即真实存在,届时回升 Medium | 二选一:补全 adjustable 三件套(accessible + accessibilityActions + onAccessibilityAction 内调 onChange ± safeStep + label),或撤掉 role/value 留纯文本,避免「承诺了调不动」 |
| **[L-30]** `src/components/ui/Radio/Radio.tsx:25-28、Stepper/Stepper.tsx:31-37` | 质量 | Low | 高(批次双层审查验真) | 渲染路径 log.warn 无去重的第 3、4 处(同 [L-10](Icon.tsx:23)、[L-11](Spinner)口径):Radio 缺 Group ctx 时每实例每次渲染都 warn(列表场景告警风暴,生产默认级恰为 warn),fallback `<View />` 连 testID 也一并丢失;Stepper 两条 warn 同处渲染体无去重,且防御口径不对称 —— step 为有限非正数才告警(:31 条件 Number.isFinite 短路),step=NaN/±Infinity 与 min/max 非有限均静默回退,而头注释(:15-16)把「step <=0 或非有限数时回退 1」写成同档行为;另 step 为小数(如 0.1)时 `{safeValue}` 原样渲染浮点伪影(0.30000000000000004),在其自述健壮性范围内未覆盖 | 并入 [L-10] 的模块级 Set 去重方案;Stepper warn 口径对齐(非有限同样告警);value 显示按 step 小数位 toFixed 或文档声明仅支持整数步进 |
| **[L-31]** `src/components/ui/TextField/TextFieldBase.tsx:100、105 ↔ Form/FormRow.tsx:21-27` | 质量 | Low | 高(批次双层审查验真) | 表单 a11y 三通道系统性缺失:(a)error 出现/变化对 SR 静默 —— 错误 Text(TextFieldBase:105、FormRow:27)无 accessibilityLiveRegion(Android 一行可补),TextInput 侧亦无任何 invalid 语义,SR 用户只有重新遍历才发现报错;(b)editable=false(非 disabled)时 accessibilityState 不标 disabled(:100 只认 disabled prop),只读输入对 SR 与可编辑无差,types.ts:23「disabled 优先于 editable」只覆盖视觉与拦截面;(c)FormRow 的 label 与 children 控件零关联(无 accessibilityLabelledBy/nativeID,types 亦无文档约定),SR 聚焦 Input 只能听 placeholder 猜字段。族内查遍无一组件处理表单 label/error 关联,属族级缺口而非单点 | error Text 补 accessibilityLiveRegion="polite"(iOS 评估 announceForAccessibility);FormRow 在 types 约定「控件需自带 accessibilityLabel(建议同 label 文案)」或提供透传;editable=false 并入 disabled state 播报 |
| **[L-32]** `src/components/ui/PasswordInput/PasswordInput.tsx:11-13、24-30 + styles.ts:4 + types.ts:9-11` | 质量 | Low | 高(批次双层审查验真) | 四连:(a)头注释「见 CLAUDE.md a11y 规范」—— 本仓 CLAUDE.md 无该节(grep 全文 a11y 字样仅 ButtonBase chrome 一处),拆库前消费者仓规范残留;(b)styles.ts:4「跟 ForgotPassword 同款 4px」—— ForgotPassword 为消费者仓屏幕名,本仓零组件(theme/tokens.ts:31 heroMd 注释同名残留,同批并入 [L-17]/[L-22] 词表);(c)inputProps 类型未 Omit secureTextEntry,只靠 ⚠ 注释保护(实现 :30 确实覆盖);且 `{...inputProps}`(:29)展开在 value/onChangeText/placeholder/testID 之后,类型面放行的同名键会反向覆盖显式 props,与注释「(只有)secureTextEntry 会被组件覆盖」的暗示相反;(d)作为密码 preset 未设输入体验默认值:textContentType(iOS 钥匙串/强密码)、autoComplete(Android autofill)、autoCapitalize='none' 全缺(对照 Search 已示范 preset 责任:returnKeyType="search"),且是输入族唯一不 forwardRef 的组件(Input/Textarea/Search 均可 focus-on-error),密码字段恰是错误聚焦高频位;(e)公开 API 无法表达 error/disabled——两者系 InputProps 自有扩展、不在 TextInputProps 内,经 `inputProps: Partial<TextInputProps>` 类型面彻底不可达,而底层 Input 两态俱备,密码字段恰是错误提示高频位 | 注释改指本仓现状并入 [L-17] 全仓残留清理;inputProps 类型 Omit<'secureTextEntry'> 使约束结构化;补三个 autofill/大小写默认值(保持可被 inputProps 覆盖)与 forwardRef;暴露 error/disabled 透传 |
| **[L-33]** `src/components/ui/Input/types.ts:22(↔ TextField/TextFieldBase.tsx:28、theme/tokens.ts:90-92)` | 质量 | Low | 高(批次双层审查验真) | height 注释「默认 44(hitTarget)」失实两层:实际默认是 control.lg = r(44)(缩放值,TextFieldBase:28),非 402pt 设备上不是 44;且 tokens.ts:90-92 明文「lg 数值与 fixed.hitTarget 一致,但 lg 是 scaled / hitTarget 是物理常量,语义不同不要合用」—— 该注释恰好犯了 token 表自己禁止的合用。触控目标合并行 [M-7] 的前身曾把本处列为 fixed.hitTarget 的「注释引用」消费证据,核实应为失实引用,表单族对 hitTarget 实际消费为零 | 注释改「默认 control.lg(r(44),随设备缩放)」;[M-7] 合并时已同步修正该枚举项 |
| **[L-34]** `src/components/ui/Segmented/Segmented.tsx:27(↔ Tabs/Tabs.tsx:22)` | 质量 | Low | 高(批次双层审查验真) | item 有 accessibilityRole="tab" + selected state,但容器 View 无 accessibilityRole="tablist"(RN 支持该 role)→ SR 用户无「这是一组几项的选择器」的分组上下文与项数信息;Tabs 容器同缺(:22;tablist 缺失属 a11y 维度、不在 [M-22] 重复维度记账范围,故此处新记);RadioGroup 容器同模式缺 accessibilityRole="radiogroup"(RadioGroup.tsx:25,item 已有 role="radio",RN 类型已支持) | 三容器补 tablist / radiogroup;与 [M-22] 的 tab-item primitive 决策同批落地 |
| **[L-35]** `src/components/ui/Form/FormGroup.tsx:11-12、29-33` | 质量 | Low | 高(批次双层审查验真) | key fallback 注释与实际机制不符:React.Children.toArray 对无 key 元素也指派位置 key(本仓 react@19.2.3 实测:无 key 元素 → ".0",有 key → ".$a")→ 对元素 child 而言 `child.key != null` 恒真,头注释承诺的「不传时 fallback 到 __row-${i}」对元素从不发生(实际兜底是 toArray 的位置 key,行为同为位置键,「动态列表须显式 key」的告诫本身仍成立);`__row-${i}` 分支仅字符串/数字 child 可达。无运行时 bug,但读者按注释排查动态列表 key 问题时会找错机制 | 注释改为如实描述(keyless 元素由 toArray 指派位置 key,语义等同索引);死分支可顺带简化 |
| **[L-36]** `src/components/ui/Search/types.ts:3 ↔ Input/Input.tsx:17、Input/types.ts:12-14` | 质量 | Low | 高(批次双层审查验真) | Search 类型面重新放行被 Input 刻意 Omit 的 multiline/numberOfLines:Search/types 只 Omit 'style',且 Input.tsx 的 `multiline={false}` 写在 `{...props}` **之前** → tsc 实证 `<Search multiline numberOfLines={4} />` 编译通过,运行时翻成 96pt 多行框、height 36 失效;PasswordInput 经 inputProps 同途走私(亦实证)。与 [M-9]、[L-32] 同根:spread 顺序 + Omit 清单不完备。 | Search/types Omit 扩为 'style' / 'multiline' / 'numberOfLines';Input/Textarea 把硬编码 prop 移到 spread 之后;PasswordInput inputProps 的 Omit 同步扩 |
| **[L-37]** `src/components/ui/Checkbox/styles.ts:7-11、Radio/styles.ts:10-15、Switch/styles.ts:14-19`(对照 Stepper/styles.ts:14、Segmented/styles.ts:12) | 质量 | Low | 高(批次双层审查验真) | 三件交互原子 Pressable 容器无 alignSelf 防拉伸:列向默认父容器(alignItems stretch)下命中区拉满整行宽,机制同 [M-8](Medium)——而同族同批的 Stepper/Segmented 已设 `alignSelf: 'flex-start'`,族内双纪律。差异化定级 Low:Checkbox/Radio 整行可点常属表单行期望行为;Switch(裸 32×20 控件)远端空白触发翻转最 surprising。 | 与 [M-8] 合并决断「交互原子是否一律自带防拉伸」:统一补 alignSelf,或在 types 文档化「命中区随父布局」行为 |
| **[L-38]** `src/components/ui/Search/Search.tsx:38-45、TextField/TextFieldBase.tsx:105` | 质量 | Low | 高(批次双层审查验真) | testID 派生覆盖度不齐两处:Search 清除按钮 Pressable 无 testID(对照 PasswordInput.tsx:34 同位 -toggle 已派生),E2E 只能靠 a11y label 定位;TextFieldBase 的 error Text 无 `-error` 派生(对照 Stepper 对非交互 `-value` 亦派生)。 | 清除按钮补 `-clear`、error Text 补 `-error`,随 [L-92] 统一决断落地 |
| **[L-39]** `src/components/ui/Card/types.ts:6、19、21-24(↔ Card.tsx:26-32、styles.ts:7-12、website/docs/components/card.mdx:4)` | 质量 | Low | 高(批次双层审查验真) | 注释失实四连:(a):6 宣称 default=「白底 + 阴影 + 边框」,实现任何 variant 默认零边框(styles.base 无 border 字段,边框仅 borderColor 传入时出现);(b):21「覆盖默认 c.outline」—— 不存在该默认边框色;(c):23「覆盖默认 1」—— 实为 2(Card.tsx:31 `borderWidth ?? 2`);(d):19「覆盖默认 radius.md」—— 实为 radius.xl(Card.tsx:26;card.mdx:4「12px 圆角」与实现一致,反证 types 注释过时)。另「仅 default/flat 有边框」与实现不符 —— borderColor 条件独立于 variant,plain 传入同样生效。 | 对照实现改写四处(若设计本意 default 应带 outline 边框,则反向改实现并连动 docs);与 [L-25](Button 注释失实三连)同批清理 |
| **[L-40]** `src/components/ui/Cell/types.ts:16 + Cell.tsx:32(↔ Leading.tsx:42-56、styles.ts:75-79)` | 质量 | Low | 高(批次双层审查验真) | 注释失实两处 + 类型边界一处:(a)types:16「IconName 自动渲染 24×24 图标」,实现 grouped 渲染裸 22、flush 渲染 16@r(28) 盒(Leading.tsx:44、53),24×24 两形态都不存在;(b)Cell.tsx:32「行间 inset hairline 分隔」,separator 实为铺满全宽(styles.ts:75 注释自言「铺满 cell 全宽」,List divider 仅 full/none 两档无 inset 档)→ 同组件两注释互相矛盾;(c)leading 的 `IconName 与 ReactNode 联合` 中 IconName 被 ReactNode(含 string)吸收,任意字符串编译合法 → 运行时按图标名走 Icon 未知 name 告警占位,types 无此边界提示。 | (a)(b) 对照实现改写;(c) 注释补「非图标名字符串会被当 IconName 处理」边界(ReactNode 吸收 string 系 TS 固有,类型层无法收窄) |
| **[L-41]** `src/components/ui/Tabs/types.ts:15-16(↔ Tabs.tsx:21-55)` | 质量 | Low | 高(批次双层审查验真) | items 注释「tab 列表(≥2 项才显示)」承诺的行为不存在:实现无任何 items.length 守卫,1 项渲染成不可切换的整行选中 tab、0 项渲染空 bar 残留底边线;同形 Segmented 无此注释 → 或是迁移前消费者侧守卫的残句,或是从未实现的意图,读者无从分辨。 | 改写为「建议 ≥2 项」语义,或补 items.length<2 时 return null(与 Segmented 同口径决断) |
| **[L-42]** `src/components/ui/Grid/Grid.tsx:28-30(↔ types.ts:17)` | 质量 | Low | 高(批次双层审查验真) | 两点:(a)渲染路径 log.warn 无去重第 5 处([L-10]、[L-11]、[L-30] 族):columns 非法时每次重渲染都告警,列表父容器高频重渲染即告警风暴,logger 生产默认级恰为 warn;(b)types:17 的 ⚠ 注释宣称类型外值「会**静默**回退到 4 列 — 业务应在传入前自行校验」,实现却 warn —— 文档与实现互斥:按注释接入的业务不会预期日志噪音,按实现理解的又会误信 ⚠ 提示。 | (a) 并入 [L-10] 模块级 Set 去重方案;(b)「静默」改「告警并回退」或真改静默,二选一对齐 |
| **[L-43]** `src/components/ui/Cell/Cell.tsx:93-96、EntryCard/EntryCard.tsx:44-45、Grid/Grid.tsx:41-42、TabBar/TabBar.tsx:33-35` | 质量 | Low | 高(批次双层审查验真) | 可按压容器显式 accessibilityLabel 只含主文案,副信息被「子树扁平化 + label 覆盖」双重屏蔽(RNGH Pressable accessible 默认 true,[L-19] 已证;iOS 无显式 label 时本会拼读子 Text,显式 label 反而收窄信息面):Cell 丢 desc 与 extra(types:12 还自荐 extra 放 Switch、Stepper 等控件 —— 可按压 Cell 下这些控件对 SR 整体不可达且外层 RNGH 内层控件嵌套手势,同 [L-27](a) 口径);EntryCard 丢 sub;Grid 与 TabBar 丢 badge 计数 —— 主导航未读数对 SR 零通道。 | label 派生时拼接副信息(如 title + desc、label + badge 条数)或 types 声明边界;Cell 的 extra 含交互控件时外壳设 accessible={false} 让子元素独立可达(或文档禁止「onPress + 交互 extra」并存),与 [L-27] 同批决断 |
| **[L-44]** `src/components/ui/Avatar/Avatar.tsx:21、30-32、DrawerHeader/DrawerHeader.tsx:18、27-29` | 质量 | Low | 高(批次双层审查验真) | imageFailed 置 true 后永不重置(无 source 键控 reset):同一实例先经历加载失败、后换新 source(列表 item 复用改 props、用户更换头像 URL),仍永远渲染字母 fallback 直到 remount —— [M-20] 把「source 变更时重置 failed」列为日后演进场景,本批核实为现状缺陷;触发需「先失败 + 后换源」两条件叠加,故 Low;两个现实放大器抬升修复优先级:(a)FlashList/recycling 类列表同实例换 props 是常态;(b)[L-46] 恰制造「同实例换 source」第二通道,两缺陷复合 = 错位且不自愈。 | 补 `useEffect(() => setImageFailed(false), [source])`(source 为对象形态时按 uri 比较)或给 Image 加 source 派生 key;与 [M-20] 抽 useImageFallback 同批落地两处一并修 |
| **[L-45]** `src/components/ui/Reveal/Reveal.web.tsx:41-45(↔ Reveal.tsx:19-28、Reveal/types.ts:6-7)` | 质量 | Low | 高(批次双层审查验真) | 双端结构分叉未声明:native 单容器(style 与 children 同层 Animated.View),web 为 View(style)> 裸 div(动画)> children 三层 —— 消费者作用于子项布局的容器样式(flexDirection、gap、alignItems)native 生效、web 被中间 block 流 div 隔断,静默分叉;types:6「容器样式」未声明该边界。div 模式系仿 ToastHost.web 先例(:23 注释自述),但 Toast 的 div 只包自有内容、无消费者 style,Reveal 把该模式带进了有公开 style prop 的通用容器。正面项:退场省略与 reanimated web 崩溃规避双端注释对齐;rAF 清理(cancelled flag + cancelAnimationFrame,:21-34)合格。 | 改走 RNW animationKeyframes 单 View 实现入场淡入([M-19] 已验证路径,顺带消掉 state + 双 rAF),或 types 注明「web 下 style 不作用于子项布局」 |
| **[L-46]** `src/components/ui/Cell/List.tsx:27-28` | 质量 | Low | 高(批次双层审查验真) | flush 分支 React.Children.toArray 后用 `Fragment key={i}` 按索引重键 —— toArray 本已为元素保留 caller key(".$key" 形态),重键后显式 key 失效,动态增删/重排 Cell 按位置 reconcile:Cell 自身无状态,实际影响为重渲染浪费与 extra 槽内控件的过渡动画错位,children 若含自带 state 的自定义节点则状态串行;grouped 分支直传 children 保留 caller key,同组件双轨。另 `.filter(Boolean)` 会把合法的 0、空串文本子节点滤掉(toArray 已滤 null/undefined/布尔),现实用法无影响。 | Fragment key 改用 `child.key ?? i`(toArray 元素恒有 key);或文档声明 flush List 仅适配静态 children |
| **[L-47]** `src/components/ui/TabBar/TabBar.tsx:12、Empty/Empty.tsx:9、Cell/types.ts:34、Avatar/styles.ts:39` | 质量 | Low | 高(批次双层审查验真) | [L-17]、[L-22]、[L-32] 拆库残留词表本批四处新命中:(a)TabBar 头注释「激活色:primary-300」与(b)Empty 头注释「primary-0 圆盘」—— 旧 token 名全 src 仅注释命中(实现分别为 c.primary、c.primaryContainer);(c)Cell types「参 CLAUDE.md a11y 章节,短 ≤8 字」—— 本仓 CLAUDE.md 无 a11y 章节,[L-32](a) 同模式第二处;(d)Avatar styles「签名与 Tag/Tool 的 paletteFor 一致」—— [L-17] 预告留待本批核对,核实:Tool 本仓零存在,Tag 的 (variant, c) 签名部分属实。 | 并入 [L-17]、[L-22] 的全仓一次性清理,词表再扩 primary-300、primary-0 |
| **[L-48]** `src/components/ui/TabBar/TabBar.tsx:24` | 质量 | Low | 高(批次双层审查验真) | [L-34]「容器缺 tablist」族第 4 处(该行枚举 Segmented、Tabs、RadioGroup,未含 TabBar):item 已有 role="tab" + selected state,容器 View 无 accessibilityRole="tablist" → App 主导航对 SR 无「N 项中第几项」的分组上下文。 | 并入 [L-34] 修复同批补 tablist |
| **[L-49]** `src/components/ui/Cell/styles.ts:23-33(+ Cell.tsx:88-106)` | 质量 | Low | 高(批次双层审查验真) | 触控目标族(已合并为 [M-7])新增成员:flush Cell 置 minHeight 0 + paddingVertical r(11),无 desc 的可按压行高 ≈ 2×r(11) + 字高 17 ≈ 39pt(低于 44,窄屏 r() 再缩;带 desc 时约 51pt 合规);grouped 形态 minHeight r(56) 合规 → 同组件双档。行宽横向充裕,仅纵向欠。 | 并入 [M-7] 族级决断:flush 可按压行补垂直 hitSlop 或 minHeight 兜底(行间 hairline 分隔下相邻 slop 重叠的 [M-7] caveat 同适用) |
| **[L-50]** `src/components/ui/NavBar/NavBar.tsx:18(↔ NavBar/types.ts:7-8、IconButton/IconButton.tsx 头注释)` | 质量 | Low | 高(批次双层审查验真) | 槽位 label 缺失被静默洗掉:`slot.accessibilityLabel ?? slot.icon` 把英文图标 token 名(如 'arrow-left')直接作 SR 朗读文案;types 注释宣称「icon-only 必填」但类型可选——IconButton 本体的设计恰是「TypeScript 强制必填」,slot 便捷 API 废掉了它要复用的组件的类型强制,错误从编译期信号变成用户可听的英文残词。 | slot 的 accessibilityLabel 类型改必填(对齐 IconButton),或 fallback 删掉 icon 名、缺失即 dev warn |
| **[L-51]** `src/components/ui/DrawerHeader/DrawerHeader.tsx:30` | 质量 | Low | 高(批次双层审查验真) | 取首字 `name.trim()[0]` 按 UTF-16 码元截取:代理对开头的 name(emoji 开头昵称、CJK 扩展区生僻字人名)取到孤立高位代理 → 56×56 品牌头像渲染 tofu;空串经 `?? '?'` 已兜住(noUncheckedIndexedAccess 之功)。 | 改码点级 `[...name.trim()][0] ?? '?'`(ZWJ 组合 emoji 仍截断,注释声明边界);与 [M-20] 的 useImageFallback 抽取同批落地 |
| **[L-52]** `src/components/ui/Grid/Grid.tsx:39-47(↔ Grid/types.ts:20)` | 质量 | Low | 高(批次双层审查验真) | onPress 可选但 Pressable 与 button 语义无条件挂载:`onPress={() => onPress?.(item)}` 恒为函数,纯展示宫格的每格仍播报「button」、按压仍闪 0.7 反馈、点击无事发生;同批两个正例反衬——Carousel.tsx:66-74 显式注释「纯展示型不标 button 避免读屏器误导」、Cell.tsx:88 / EntryCard.tsx:40 均按 onPress 分支决定是否包 Pressable,Grid 是本批唯一违例。 | 按 onPress 分支渲染 Pressable / View(对齐 Cell/EntryCard 模式) |
| **[L-53]** `src/components/ui/Reveal/Reveal.web.tsx:36-39(↔ Reveal.tsx:21-22、reanimated BaseAnimationBuilder.ts:14)` | 质量 | Low | 高(批次双层审查验真) | reduce-motion 双端分叉家族第 4 例(方向与 Spinner 相反):native 的 FadeIn/FadeOut 经 layout-animation 默认 ReduceMotion.System,系统减弱动效时跳过淡入、内容即时呈现——恰是正确的 reduced 行为;web 的 CSS transition 无 prefers-reduced-motion 查询,照常动画 → 仅 web 端不合规且分叉未声明。注意:[L-45] 建议的 animationKeyframes 改造不会顺带修复此项。 | 并入 [H-3] 的「系统 a11y 开关」专项同批对齐(web 补 matchMedia 查询) |
| **[L-54]** `src/components/ui/Toast/toast.ts:22(↔ ToastHost.tsx:39、ToastHost.web.tsx:33、Confirm/ConfirmHost.tsx:41)` | 质量 | Low | 高(批次双层审查验真) | 注册表基数 0/2+ 双向无守护亦无 dev 信号:0 Host 时 toast() 静默吞(toast.ts 连 logger 都未引入,对比同族 confirm 重入有 log.warn、Spinner 入参有 warn 的纪律),集成漏挂 Host 零提示;2+ Host 时(违反「挂一次」注释契约但类型/运行时均不拦)toast 同位双份叠渲,confirm 双原生 Modal 同时 present(iOS 多 Modal 行为本就不可靠)且先 resolve 一侧关闭后另一侧残留幽灵对话框,需再点一次才关(Promise 已 settle,二次 resolve 无害)。 | emit 时 `_subs.size === 0` → dev warn「Host 未挂载,消息被丢弃」;Host 订阅时发现 `_subs.size > 0` → dev warn 重复挂载;均 `__DEV__` 限定 |
| **[L-55]** `src/components/ui/Toast/toast.ts:14、19(↔ types.ts:20)` | 质量 | Low | 高(批次双层审查验真) | duration 异常输入未防御:`Infinity`(消费者想要常驻 toast 的自然尝试)与 NaN 经 setTimeout 钳为立即触发 → 反向变成瞬时消失;0/负数同。types.ts:20 只说「自动消失毫秒数,默认 3000」未声明合法域。对照 Spinner 对 size/thickness 的 Number.isFinite + warn + 钳制先例([M-24]),同库入参防御纪律不一致。 | emit 处 sanitize:钳制域取 `[1, 2^31)`(setTimeout 32 位回绕,2^31 以上的有限值同样立即触发,仅查 isFinite 拦不住)→ 域外回落 3000 + dev warn;或把 `duration: Infinity` 定义为「不自动消失」并文档化(此时 Host 需跳过 setTimeout) |
| **[L-56]** `src/components/ui/Toast/ToastHost.web.tsx:84(↔ ToastHost.tsx:53-58)` | 质量 | Low | 高(批次双层审查验真) | reduce-motion 双端分叉家族第 5 例:native 入/退场 withTiming 默认 ReduceMotion.System(reanimated 4.4,[H-3] 已引库内源码),系统减弱时动画瞬时到端值、退场回调 finished=true 正常走 setEntry(null) —— 语义完整且合规;web CSS transition(:84)无 prefers-reduced-motion 查询照常动画 → 仅 web 不合规,分叉未声明。toast 属 transient 提示,瞬时呈现/消失即正确 reduced 形态,无 Spinner 式「essential motion」豁免争议。 | 并入 [H-3]/[L-53] reduce-motion 专项同批对齐(web 补 matchMedia 查询,reduced 下直接跳 visible 端值) |
| **[L-57]** `src/components/ui/Toast/styles.ts:14-17 ↔ Confirm/ConfirmHost.tsx:41-47` | 质量 | Low | 高(批次双层审查验真) | toast 被 confirm 的 Modal 无条件遮蔽(质量复审补录):RN Modal 是原生 window 级,toast 的 zIndex 200 永远在其下;confirm 打开期间任何后台 toast(异步完成、推送)整个生命周期不可见即消失,web 端 RNW Modal portal 到 body 末尾同理。styles.ts 注释只声明「navigator/overlay 之下」(兄弟层语境),跨族遮蔽无人记录,而 confirm.mdx:113 还引导「信息提示用 toast()」。 | 文档声明该层级关系(confirm 打开时勿依赖 toast);长线评估 toast 改 Modal 承载或 confirm 打开时排队 toast |
| **[L-58]** `src/components/ui/Toast/types.ts:26` | 质量 | Low | 高(批次双层审查验真) | `id` 字段注释「用于动画切换识别」是承诺失实:三个 Host 零处读 `entry.id`(grep 坐实)——与 [L-101] 同族;若 [M-14] 的 id 守卫修法落地则该承诺顺带变真。 | 随 [M-14] 修复落地(id 真被消费),或注释改「预留给守卫/去重」 |
| **[L-59]** `src/components/ui/BlurLayer/BlurLayer.tsx:12`(跨批补漏,BlurLayer 批次范围) | 质量 | Low | 高(批次双层审查验真) | 头注释「'strong' 焦点引导 backdrop(BottomSheet blur 模式)」引用已删除组件:BottomSheet 于 41f1f30 删除([L-72] 已考古坐实),src 内该词唯一命中即此注释——是该行修复清单(CLAUDE.md + migration.md)覆盖不到的第三处同根残留,残留清理词表应扩入 BottomSheet。 | 注释改「sheet 类浮层 backdrop」;并入全仓残留清理,词表扩 BottomSheet |
| **[L-60]** `src/components/business/Decorations/ScreenBackdrop.tsx:105(↔ RadialHalo.tsx:38-39、Decorations/types.ts:60)` | 质量 | Low | 高(批次双层审查验真) | 固定字面量 gradientId 打穿子组件的 useId 防碰撞机制:RadialHalo 默认 `rh-${useId 消毒}` 自动唯一(GradientWash 同款 :42-43),ScreenBackdrop 却显式传 `screen-backdrop-halo-${i}` —— 实例内唯一、跨实例必同名,types.ts:60 自家契约「同屏多 halo 必须不同 id」在多实例同挂场景(导航栈 keep-mounted、同页多 demo)被自己违反。原生 iOS 端经核无害 —— react-native-svg 的 painter 注册是每个 Svg 根的实例方法(RNSVGSvgView.h:50-52 definePainter,per-root 作用域);web 端 DOM SVG id 按规范文档级全局,`url(#…)` 命中文档首个同名 → 两实例 halo 配置不同(自定义 color/maxOpacity)时后挂实例渲染成先挂实例的渐变。现触发面窄(仅 1 个 preset、decorations.mdx 现仅 1 个 live 实例,且同 preset 双实例 def 相同视觉无差——串台须差异配置)故从低;升级条款:docs 站增第二个差异配置 live demo、或消费端 web 多屏差异配置出现,即升 Medium。同文件 GradientWash 调用(:89-93)未传 id 走 useId,同组件两套 id 策略并存。 | 删 :105 的 gradientId 传参,halo 走 RadialHalo 默认 useId(「同实例多 halo 不同 id」useId 本就保证);若须显式 id,前缀拼入组件级 useId |
| **[L-61]** `src/components/business/Decorations/GradientWash.tsx:11-22(对照 :23-27、:40)` | 质量 | Low | 高(批次双层审查验真) | 组件级 docblock 错挂孤置:11-22 是 GradientWash 的使用文档(两种用法、pointerEvents 行为、useId 消毒说明),但其下方紧跟的是 parseRgba 自己的 docblock(:23-27)与函数体(:28),而 :40 `export function GradientWash` 上方无任何注释 —— JSDoc 就近归属规则下组件文档不属于任何声明,IDE hover GradientWash 看不到用法文档。 | :11-22 整块下移至 :40 组件声明上方 |
| **[L-62]** `src/components/business/Decorations/GradientWash.tsx:28-37(↔ Decorations/types.ts:7、ScreenBackdrop.tsx:28)` | 质量 | Low | 高(批次双层审查验真) | alpha 防御覆盖窄于类型契约:types.ts:7 宣称 color 接受「任意有效颜色字符串(hex / rgb / rgba / hsl ...)」,parseRgba 却只拆逗号式 `rgba()`;按其 why 注释自述的前提(:26-27,react-native-svg Stop stopColor 只解析 RGB、alpha 可能被忽略),`#RRGGBBAA`、`hsla()`、空格斜杠语法的 alpha 会被静默丢弃(渲染为全不透明)。另 :65 `s.opacity ?? parsed.opacity` 是替换而非相乘 —— ScreenBackdrop.buildStops(:28)显式 opacity:1,日后 palette 若含 rgba 色其 alpha 将被 1 覆盖。现仓内 palette/token 形态(hex 与逗号式 rgba)均未触雷,纯边界缺口。 | parseRgba 扩 8 位 hex 与 hsla,或 types.ts:7 契约收窄为「hex / rgb / rgba(逗号式)」;buildStops 的 opacity:1 改为不传,留给 parseRgba 提取 |
| **[L-63]** `src/components/business/Decorations/(目录形态,对照 CLAUDE.md 组件约定节)` | 质量 | Low | 高(批次双层审查验真) | 组件目录约定唯一偏离点:CLAUDE.md「每个组件一个独立目录,固定结构(ComponentName.tsx/types.ts/styles.ts/index.ts)」,Decorations 三组件共居一目录、共享一份 types.ts、全员无 styles.ts(样式走文件内 StyleSheet + inline 派生)—— business 其余三件均守约。族式聚合本身合理(三件强内聚、共享 GradientStop),但偏离无 why 注释亦无 CLAUDE.md 例外声明,新增组件时两套结构先例并存。types.ts(103 行)经查为纯类型 + 文档注释(判别联合用 `?: never` 系类型级技巧),未塞实现逻辑;types.ts:65 的 palette 端点 hex 注释与 palettes.ts:21-22 逐值核对一致。 | CLAUDE.md 组件约定节补一句「强内聚组件族可共目录(现例 Decorations)」,或 Decorations/index.ts 头注释声明族例外 |
| **[L-64]** `src/components/business/AvatarWithRing/types.ts:11(↔ AvatarWithRing.tsx:31、49-53,theme/colors.ts:88、180)` | 质量 | Low | 高(批次双层审查验真) | ringColor 注释「ring + 填充背景色,默认 c.primary」两处失实:实际默认是 `c.avatarRing`(亮 rgba(255,255,255,0.6) / 暗 rgba(255,255,255,0.10),白系半透明,非品牌橙);且该色仅作外壳 ring 视觉 —— 中心填充是 avatarGradient SVG 渐变(:49-53),ringColor 不参与填充。同组件头注释(.tsx:14「白色 ring」)与 styles.ts:20 均为现状口径,唯独 types 这份消费者选色时实际会读的注释失实。 | 注释改「ring 颜色,默认 c.avatarRing(白系半透明)」 |
| **[L-65]** `src/components/business/GlassStats/GlassStats.tsx:27(↔ GlassStats/types.ts:5-6)` | 质量 | Low | 高(批次双层审查验真) | 列表 key 取 label 而 types 未约束唯一:`key={label}` 在 items 含同名 label 时触发 React 重复 key 告警与 Fragment 复用错位(count 不参与 key,同 label 不同 count 的列被视为同一元素);types 仅注「一般 2~4 列均可,不强制数量」,无唯一性契约。 | key 改 `${label}-${i}`(纯展示列表无重排,带 index 安全),或 types 注明 label 须唯一 |
| **[L-66]** `src/components/business/VersionPill/VersionPill.tsx:19、23` | 质量 | Low | 高(批次双层审查验真) | a11y 两处:(a)状态点语义仅颜色通道 —— statusColor 默认 c.success,无任何文字对应,SR 不可达([L-14] 同族,信息件缺补救入口);(b)「·」分隔符是独立 Text 节点,SR 逐节点朗读时读出无语义符号,且 pill 整体未分组(无容器 accessibilityLabel),「版本 2.8.0」「·」「build 123」三段分读。 | 容器 `accessible` + accessibilityLabel 合并(如「版本 2.8.0,build 123」+ 状态语义),或并入 [L-14] 信息件 a11y 边界决断 |
| **[L-67]** `src/components/business/VersionPill/styles.ts:12 ↔ src/theme/tokens.ts:22` | 质量 | Low | 高(批次双层审查验真) | 拆库残留词表新命中 ×3([L-17]/[L-22]/[L-47] 同根):两处注释宣称 microPlus「与 SplashScreen / profileCard / carousel 共用」「VersionPill / profileCard / Splash / carousel 副标题」—— SplashScreen / profileCard 本仓零组件(全 src 唯二命中即这两条注释自身);`Splash` 一词另有 5 处合法场景注释命中(Decorations/types.ts:66、ScreenBackdrop.tsx:36、palettes.ts:18、shadow.ts:67、:75——描述「开屏场景」语义,非组件引用),清理时不可机械按词删;microPlus 全 src 实际消费者仅 VersionPill(styles.ts:32、39),连 carousel 都不消费 → 「共用」在本仓完全失实,误导 token 调整时的影响面评估。 | 两处注释改「现仓内仅 VersionPill 消费」;并入 [L-17] 全仓残留清理,词表扩 SplashScreen / profileCard(`Splash` 单词因场景注释合法命中多,须人工逐条甄别) |
| **[L-68]** `src/components/business/Decorations/RadialHalo.tsx:64(↔ GradientWash.tsx:26-27、types.ts:52、76)` | 质量 | Low | 高(批次双层审查验真) | `stopColor={color}` 裸传不经 parseRgba(质量复审补录):按同目录 GradientWash 自述前提(react-native-svg 的 Stop stopColor 只解析 RGB、alpha 可能被忽略),消费者经 RadialHaloProps.color / ScreenBackdropHalo.color(types 均不限格式)传 rgba 时 native 丢 alpha(halo 全强度)、web 按规范相乘(变暗)→ 双端分叉;现仓内默认 c.primary 为 hex 不触雷。同文件对 opacity 的处理(:60 stopOpacity)反而正确。 | RadialHalo 复用 parseRgba(连同其契约修复),或 types 注明「仅支持 hex」 |
| **[L-69]** `src/components/business/AvatarWithRing/styles.ts:31-32` | 质量 | Low | 高(批次双层审查验真) | size 零防御:size=0/负 → `inner = r(size) − ringWidth×2` 为负,负宽高直入样式与 `<Svg width={inner}>`;NaN 全链 NaN;合法小值 size≤4 也得 inner≤0(核心圆消失)。对照同库 Spinner 的 isFinite + warn + 钳制先例([M-24])与 [L-55],入参防御纪律不一致的 business 批次延伸。 | 并入族级 sanitize 决断:钳制 size ≥ ringWidth×2 + 1 并 dev warn |
| **[L-70]** `src/components/business/VersionPill/VersionPill.tsx:21(↔ types.ts:7)` | 质量 | Low | 高(批次双层审查验真) | `{build ? …}` truthy 判定使 `build="0"` 被静默省略,违 types.ts:7「为空时省略」契约('0' 非空);与 [L-8] 同族,触发概率低。 | 改 `build != null && build !== ''` 判定,或 types 声明「falsy 即省略」 |
| **[L-71]** `website/docs/components/toast.mdx:16`(范围外顺带发现,移交 docs 维护) | 质量 | Low | 高(批次双层审查验真) | 实时预览声明失实:自称渲染「ToastHost.tsx 本体(reanimated 4 worklet 驱动)」,但 website webpack 把 `.web.tsx` prepend 到解析扩展(docusaurus-rnw/index.js:86-96),demo 实际跑的是 ToastHost.web.tsx(纯 CSS)——docs 是 llms.txt 唯一来源,失实会直接喂给 AI 消费侧;且该 demo 跑的恰是 [H-6](web 定位缺失)的坏文件。 | toast.mdx 改写为如实描述(预览为 web 分叉版);docs 范围外,随修复批次移交 |
| **[L-72]** `CLAUDE.md:45 + website/docs/migration.md:104-110(Step 4 附加核查,与 [L-97] 互链;migration.md 部分系范围外顺带,移交 docs)` | 质量 | Low | 高(批次双层审查验真) | 浮层族「删除/恢复」两段史实未同步文档:(a)CLAUDE.md:45 的 ./components/ui 列举仍含 BottomSheet —— git 考古坐实其于 `41f1f30`(0.6.0,去 @gorhom)删除且此后未重建(src/components/ui 现 37 目录无之,全历史仅 b090723 迁入、41f1f30 删除两笔)→ 系文档漂移而非待补组件,[L-97] 预告的核实结论;(b)migration.md:106-110 仍称 confirm「0.6.0 已移除,不再从包根导出」(「自持 confirm()」建议在 :116),但 confirm/ConfirmHost 已于 `5959980`(#66,裸 Modal 版,落入 0.7.0)恢复并经 ui/index.ts:17 从包根导出 —— docs 是 llms.txt 唯一来源,AI 消费侧会被引导绕开现存 API。 | CLAUDE.md:45 删 BottomSheet 一词;migration.md 0.6.0 节补「confirm 已于 0.7.0 恢复(裸 Modal 版)」并指向 confirm.mdx |
| **[L-73]** `src/icons/data.ts`(整文件,↔ `package.json`) | 最佳实现 | Low | 高(批次双层审查验真) | 闭集 Record 形态的固有体积权衡未被显式记录:113 图标 41,664 字节以单一 `ICONS` Record 导出,任何经根 barrel 的消费全量载入;web 打包器无法 per-icon 摇树(对象属性不是模块边界),叠加 package.json 无 `sideEffects` 字段、exports 无子路径,连模块级摇树信号也缺失。主战场 Metro 本就全量打包,41KB 源码级体量现状可忽略;成本随图标目录线性增长,远期才需 per-icon 模块化。 | 短期只补 `package.json#sideEffects: false`(独立小改,惠及全包 web 摇树);per-icon 模块化留到图标量级显著增长时再评估 |
| **[L-74]** `src/components/ui/` 基础原子默认尺寸(代表行:Spinner.tsx:17、19,Skeleton.tsx:22、38-45,PulseDot.tsx:8,StatusDot.tsx:15,Logo.tsx:16,Thumbnail/styles.ts:14、16) | 最佳实现 | Low | 高(批次双层审查验真) | 与 [M-18](Icon.tsx:13)同根的缩放纪律缺口在原子默认值面成片存在:Spinner 18/2(双端)、Skeleton 40/11/3/80/8、PulseDot 6、StatusDot 16、Logo 64 全为裸常量不入 r();而同批 Thumbnail 的 sizingFor 三档全 r()(styles.ts:20 注释自述「全 r() 缩放」)、StatusDot borderWidth 又走 r(1.5)、Thumbnail ring 的 2/1 再裸 → 同批原子两套纪律并存甚至同组件内混用,非 402pt 设备上 rf 文本与这些原子比例漂移。 | 与 [M-18] 同一决断统一处理:确定「原子默认尺寸是否应缩放」后全批迁移(r() 包裹或显式豁免注释),边界记入 CLAUDE.md |
| **[L-75]** `src/components/ui/StatusDot/types.ts:9-15`、`src/components/ui/Pulse/types.ts:12-22`(对照同批其余原子) | 最佳实现 | Low | 高(批次双层审查验真) | 同批 8 件原子中 Spinner/Skeleton/Logo/Thumbnail/BlurLayer 5 件暴露 `style` prop,StatusDot/Pulse/PulseDot 3 件不暴露且无 why 注释——布局微调(margin 等)必须额外包 View,批内 API 面两套纪律并存(与本批「同批两套纪律」判据同口径)。 | 三件补 `style?: StyleProp<ViewStyle>` 透传,或在 types 注明刻意不收 style 的边界理由 |
| **[L-76]** `src/components/ui/Tag/styles.ts:20-31(对照 :6-16、Button/styles.ts:35-61)` | 最佳实现 | Low | 高(批次双层审查验真) | Tag `sizingFor` 全裸数值不入缩放体系:h 22/26、px 8/10、fs 12/13 绕过 r()/rf(),而现成 token 逐一对应(t.xxs=rf(12)、t.xs=rf(13)、space['3']=r(8)、space['4']=r(10));同文件 base 的 `radius.sm` 又是 r() 缩放(:11)→ 同组件圆角随设备缩放、盒高/字号恒定,非 402pt 设备上比例自漂;同族 Button `sizingFor` 全走 token(control/space/t/radius + r(18))。系 [L-74](基础原子默认尺寸,代表行 Spinner.tsx:17 等)同根缺口在按钮族批次的延伸 | 并入 [L-74] 同一决断统一处理:改 token(h: r(22)、px: space['3']、fs: t.xxs)或显式豁免注释;types.ts:17「md=22 (h)」文档随之标明是否物理恒定 |
| **[L-77]** `src/components/ui/IconButton/types.ts:5-28(对照 Button/types.ts:38-41)` | 最佳实现 | Low | 高(批次双层审查验真) | IconButton 无 `loading` prop:ButtonBase 已实现(square 下 spinner 换入且宽度稳定)、Button 已暴露,同 base 双消费者单侧缺口且无 why 注释——异步图标操作(刷新/收藏)是常见场景。与「同批 API 面两套纪律」判据同口径。 | 透传 `loading` 一行补齐,或 types 注明刻意不支持的理由 |
| **[L-78]** `src/components/ui/Chip/types.ts:4-15(对照族内其余交互组件)` | 最佳实现 | Low | 高(批次双层审查验真) | Chip 是按钮族交互组件中唯一无 `disabled` 的(Button/IconButton/Switch/Radio/Checkbox 均有;表单族 Segmented/Tabs 亦缺,见 [L-82]),可按压分支亦无透传口,「禁用的筛选项」无法表达——API 缺口而非缺陷。 | 补 `disabled?`(透传 Pressable + 视觉 0.5 + accessibilityState,与 ButtonBase 口径一致);若收进 ButtonBase 则自动获得 |
| **[L-79]** `src/components/ui/Checkbox/Checkbox.tsx:43(↔ Checkbox/styles.ts:13-14)、Radio/styles.ts:19、28、Switch/styles.ts:23、29(↔ theme/tokens.ts:60、67)` | 最佳实现 | Low | 高(批次双层审查验真) | 五处「圆形/胶囊」全靠 尺寸/2 与 radius token 的数值巧合成立:Checkbox circle 形态 inline `r(20) / 2` 跨文件复刻 styles 的盒尺寸 r(20)(派生魔数不同源,同 [L-94] 口径);Radio 外圈 borderRadius=radius.lg(r(10))恰为盒 r(20) 之半、内点 radius.xs(r(4))恰为 r(8) 之半;Switch track=radius.lg 恰为 TRACK_H/2、thumb=radius.md 恰为 THUMB/2 —— 任一尺寸单独调整即静默变圆角矩形;而 tokens 已备 radius.pill = 999(「big-enough sentinel,不缩放」)正为此用,表单族零消费;tokens.ts:60 注释还把 radius.xs 标注为「Radio/Checkbox/Tag/Citation 内圈 4pt」,把巧合半制度化(同 [M-18]「文档把裸值制度化」口径;Citation 系拆库残留词,并入 [L-17] 词表) | 五处圆形改 radius.pill(或在各自 styles.ts 内按 size/2 同源派生);tokens.ts:60 注释随之修正 |
| **[L-80]** `src/components/ui/Checkbox/types.ts:2-5 ↔ Switch/types.ts:2-3(+ Radio/RadioGroup.tsx:24)` | 最佳实现 | Low | 高(批次双层审查验真) | 族内受控 API 两处纪律分裂:(a)同为布尔开关,Checkbox 用 checked+onChange、Switch 用 value+onChange,族内其余受控件(Radio.Group/Segmented/Stepper/TextField 系)均 value 系 —— Cell 行尾 switch↔checkbox 互换需改 prop 名;a11y 注入口同样分裂:Switch 暴露 accessibilityLabel(types.ts:6),Checkbox/Radio 不暴露(label 兼任,box-only 形态 SR 零标签且无补救 prop,同 [L-14] 口径);(b)RadioGroup 的 Provider value 是内联对象字面量(每渲染新引用)→ 父级任意重渲染令全部 Radio 子项连带重渲染,与 ThemeProvider「useMemo 稳定引用是缓存契约」的仓内既定纪律(CLAUDE.md)不一致 —— Radio 子项渲染廉价,记纪律不一致而非性能缺陷 | 布尔受控命名留 breaking 窗口统一决断(倾向 value 系);Checkbox/Radio 补可选 accessibilityLabel;RadioGroup value 套 useMemo([value, onChange, testID]) |
| **[L-81]** `src/components/ui/TextField/TextFieldBase.tsx:29 + Search/Search.tsx:29` | 最佳实现 | Low | 高(批次双层审查验真) | [L-74]/[L-76] 同根缩放纪律缺口在表单族的两处延伸:TextFieldBase 单行高默认 control.lg(缩放)而 multiline minHeight 默认裸 96,同组件双轨(Textarea/types.ts:20 自述「约 5 行」—— 字号 t.body=rf(15) 随设备缩放而 96 恒定,非基准设备上「5 行」自漂);Search 把高度覆盖为裸 36,现成 token control.md=r(36) 零消费 → Search 形态不缩放、Input 默认形态缩放,同一输入框双纪律 | 并入 [L-74] 同一决断:minHeight 改 r(96) 或按行数 × 行高派生;Search 改 height={control.md} |
| **[L-82]** `src/components/ui/Segmented/types.ts(↔ Tabs/types.ts)` | 最佳实现 | Low | 高(批次双层审查验真) | Segmented 无 disabled(整体与 per-item 均无),Tabs 的 TabItem 同缺——「禁用的选项卡/分段」无法表达;与 [L-78](按钮族)同口径的跨族延伸。 | 补整体 `disabled?` 与 per-item `disabled?`(a11y state 同步);与 [M-22] 的 tab-item primitive 决策同批 |
| **[L-83]** `src/components/ui/DrawerHeader/styles.ts:15-17、26-27 + Tabs/styles.ts:26(↔ theme/tokens.ts:79-88)` | 最佳实现 | Low | 高(批次双层审查验真) | tokens.ts:79-80 明文「icon['2xl'] 与 avatar.xl 同值但语义不同(图标 vs 头像),业务按语义选不要混用」:DrawerHeader 头像盒(头注释自述「56×56 单字头像」)恰用 icon['2xl'] 五处取值,语义对应的 avatar.xl 在 DrawerHeader 内零消费(Avatar/styles.ts:35 自身有正当消费) —— 正中 token 注释禁止的那种混用;Tabs 的 underBar 宽度(下划线长度)取 icon.xl(r(36)),与图标盒语义无关、纯数值巧合,任一阶梯日后调整即连带异物。[M-18] 已记 TabBar.tsx:43 拿 icon.sm 当字形为第三式。对照正当用法不计:Empty illust 用 icon['2xl']、EntryCard iconTile 用 icon.lg 均为 IconBox 本义(浅底 + 中心 icon)。 | DrawerHeader 改 avatar.xl;Tabs underBar 宽改 r(36) 或组件内具名常量;与 [M-18] 的 icon.* 语义决断同批 |
| **[L-84]** `src/components/ui/Cell/styles.ts:46、49、58、68、EntryCard/styles.ts:32、38、Avatar/styles.ts:24-37` | 最佳实现 | Low | 高(批次双层审查验真) | [M-18]、[L-74]、[L-76] 同根缩放纪律缺口在本批三式:(a)Cell flush 形态字号与行高走 r(13)、r(11) 线性缩放,同组件 grouped 形态走 rf 系 token(t.body、t.xs)→ 同一 Cell 两形态在非 402pt 设备上字号缩放律不同;(b)EntryCard 同走 r(13)+0.5、r(11)(types:7、9 把「13.5px、11px」写进 API 文档,同 [M-18] 文档制度化口径;设计稿专定注释只解释 0.5 不解释 r-vs-rf 选型);(c)Avatar sizingFor 的 fs 五档全裸常量(10-20)而盒走 avatar.* 的 r() 缩放 → 盒缩字不缩,字盒比例自漂([L-76] Tag 行的 Avatar 版)。对照正例:NavBar title 用 t.h3+0.5(rf token + 绝对微调,styles.ts:39-41 有 why 注释)。 | 字号统一 rf() 或 type token(设计稿微调以绝对差表达,如 rf(13)+0.5);并入 [M-18]、[L-74] 同一决断批 |
| **[L-85]** `src/components/ui/Carousel/Carousel.tsx:2、53-79` | 最佳实现 | Low | 高(批次双层审查验真) | 包装边界三件(自有定时器为零 —— autoPlay 全委托库,经库源核验清理与手势竞态合格:useAutoPlay cleanup→pause + stopped 守卫(useAutoPlay.ts:46-51),onTouchBegin、onScrollStart→pause 与 onTouchEnd、onScrollEnd→start(CarouselLayout.tsx:141-153)):(a)不 forwardRef —— 库 ICarouselInstance(scrollTo、next、prev)与 autoplay pause、start 全不可达,「跳到第 N 张」与 WCAG 2.2.2「自动轮播用户可暂停」在消费端无法实现,只能整体不开 autoPlay;(b)loop 恒开硬编码(:57)无 prop 无注释,「到尾即停」的 onboarding 轮播形态被关死;(c)单项 Pressable 用 RN 版(:2)而全仓交互件统一 RNGH Pressable 且此处无 pressed 视觉反馈 —— 嵌在库 RNGH pan 手势内或系刻意规避冲突,但零注释(违仓库 why 注释标准)。 | forwardRef 透传库 ref(a11y 暂停能力随之解锁);loop 暴露为 prop 或注明边界;Pressable 选型补一句 why |
| **[L-86]** `src/components/ui/NavBar/NavBar.tsx + NavBar/types.ts(↔ TabBar.tsx:13、website/docs/components/tabbar.mdx:58、navbar.mdx)` | 最佳实现 | Low | 高(批次双层审查验真) | 安全区归属两组件一致(都不处理 insets、留给消费者)但声明单侧缺失:TabBar 代码头注释(:13「想留 home indicator 的安全区,请外面包 SafeAreaView edges=['bottom']」)与 tabbar.mdx:58 双声明;NavBar 高 fixed.navbarH=44、顶贴状态栏,代码注释、types、navbar.mdx 三处对 top inset 零提示(grep 安全区、SafeArea、insets 全 0 命中)→ 消费者首接 NavBar 顶进状态栏后才能发现归属约定。正面项:fixed.navbarH、fixed.tabbarH 直用不二次 r() 双双合规(NavBar/styles.ts:8、TabBar/styles.ts:8)。 | NavBar 头注释 + navbar.mdx 各补一句「top inset 由消费者包 SafeAreaView edges=['top']」,与 TabBar 声明对齐 |
| **[L-87]** `src/components/ui/{Avatar,Grid,NavBar,TabBar,Tabs,DrawerHeader,Empty}/types.ts(对照同批 Card、Cell、List、EntryCard、Carousel、Reveal)` | 最佳实现 | Low | 高(批次双层审查验真) | [L-75]「style prop 暴露两套纪律」在本批延续:12 目录中 7 件不暴露 style 且无 why 注释,5 件暴露(Card、Cell 目录的 Cell 与 List、EntryCard、Carousel、Reveal)—— Avatar、Empty 是常嵌进排版流的原子,边距、对齐微调必须额外包 View;NavBar、TabBar、DrawerHeader 等全宽结构件不暴露尚可论证但同样未声明。 | 与 [L-75] 同一决断:批量补 `style?: StyleProp<ViewStyle>` 透传,或在 types 注明刻意不收 style 的边界 |
| **[L-88]** `src/components/business/Decorations/ScreenBackdrop.tsx:42-44` | 最佳实现 | Low | 高(批次双层审查验真) | warmOrange preset 的 halo 几何全裸 px 不入 r()(520/420/-120、560/380/-160、220/180/-80):同层 business 组件几何普遍走 r()/token(AvatarWithRing styles 全 r()、VersionPill r(11)/r(5)、GlassStats 走 space/radius),同组件内 GradientWash 高度取 winH 动态适配而 halo 定值 —— 非 402pt 设备上装饰占比漂移(320pt 窄屏 520 宽 halo 占比 162%,402 基准为 129%);纯装饰层无功能影响,且无「刻意绝对尺寸」why 注释。 | 并入 [M-18]/[L-74]/[L-84] 缩放纪律统一决断:halo 几何 r() 包裹,或注释声明装饰层刻意用绝对尺寸 |
| **[L-89]** `src/components/business/GlassStats/types.ts:4-7 + business 四组件 types 全量(对照 ui 层)` | 最佳实现 | Low | 高(批次双层审查验真) | business 层 API 面两处纪律缺口([L-75]「同批两套纪律」同口径):(a)GlassStats 是 business 4 组件中唯一不暴露 `style` 的(AvatarWithRing/VersionPill/Decorations 三件均有,Decorations 三个 Props 亦全有),外边距/定位微调须额外包 View,无 why 注释;(b)testID 全层缺位 —— business 全部文件 grep 0 命中,而 ui 层 37 份 types.ts 中 32 份含 testID,E2E 定位 business 组件只能依赖外包容器。 | GlassStats 补 style 透传;business 四件统一补 testID,或在 business barrel 头注释声明该层不承诺 testID 的边界 |
| **[L-90]** `src/theme/shadow.ts:7 ↔ src/theme/colors.ts:5、75、111、196` | 重复 | Low | 高(批次双层审查验真) | 品牌橙 hex `'#EB6E00'` 字面量跨文件重复 5 处:colors.ts 亮/暗 `primary`、`glassActiveFg`(:75,注释自言「亮色同 primary」)、`avatarGradient` 第 2 stop、shadow.ts 模块级 `BRAND`(全部品牌光晕 shadowColor 的唯一来源)—— 品牌色调整需多点手工同步,漏改 shadow.ts 即光晕与主色静默分叉(无类型/测试兜底);另有 8 处 `rgba(235,110,0,…)` 同色字面量受字符串 token 形态限制难以引用常量,不计入。 | colors.ts 导出单一品牌常量(如 `BRAND_ORANGE`),亮/暗 `primary`、`glassActiveFg`、`avatarGradient`、shadow.ts `BRAND` 共同引用(primary 亮暗同值,scheme 无关推导安全) |
| **[L-91]** `src/icons/svg/mic.svg ↔ mic-on.svg` | 重复 | Low | 高(批次双层审查验真) | 两文件字节级相同(diff 为空),ICONS 中同一字形占两个 name;别名意图无注释/文档可考,改 mic 字形时易漏改 mic-on(反之亦然) | 若刻意别名,在其一加注释声明同源(或脚本支持 alias 映射);否则删一保一并在消费端归一 |
| **[L-92]** `Toast/ToastHost.tsx:95、Toast/ToastHost.web.tsx:96、TextField/TextFieldBase.tsx:101、Stepper/Stepper.tsx:56、70、83、PasswordInput/PasswordInput.tsx:34`(均 `src/components/ui/` 下,5 文件 7 处;对照 `src/utils/testID/testID.ts:16-23`) | 重复 | Low | 高(批次双层审查验真) | `childTestID` 自述把「父 testID + 子 id → 子 testID」拼接收口,CLAUDE.md 亦定它为替代 inline 三元的标准助手;列表型 4 组件(Tabs/Segmented/TabBar/Grid)已接入,但 src 内仍残留 7 处固定子项的同构 inline 三元 ``testID ? `${testID}-text` : undefined``(后缀 -text/-input/-decrement/-value/-increment/-toggle),分布 5 文件。helper 省略 override 实参时与该三元逐字等价(`childTestID(testID, 'text')` 即 drop-in,返回类型同为 string 或 undefined),两种写法并存;现 7 处模式统一、无行为分叉,纯一致性问题。另:`Radio/Radio.tsx:31` 是第 8 处残留且形态特殊——`testID ?? (ctx.groupTestID ? ... : undefined)`,正是 testID.ts:8 头注释逐字所引的三参旧模式在 src 内唯一活体(Radio 实为第 5 个列表型组件却未接入 helper);它同时是 [L-8] 所记 `''` 边界差异的现实佐证:直接替换为 `childTestID(ctx.groupTestID, value, testID)` 会在 `testID=''` 时行为分叉(旧式保留空串,helper 回落拼接)。第 9 处 `Carousel/Carousel.tsx:65` 行为分叉更大:``${testID ?? 'carousel'}-item-${index}`` 在父 testID 缺失时不返 undefined 而硬编码 `'carousel'` 兜底恒产出——同屏两个未传 testID 的 Carousel 会碰撞出相同的 `carousel-item-N`,且其 types.ts:34 的 testID 无派生规则注释(其余列表型组件均有);不可机械替换,须先决断语义。 | 7 处统一替换为 `childTestID(testID, '<child>')`;Radio.tsx:31 替换时须先决断 `''` 边界语义(见 [L-8]);Carousel.tsx:65 先决断「恒产出兜底」是否刻意(删兜底对齐 helper 契约,或补 why 注释);或若刻意把 helper 限定列表型场景,在 testID.ts 注释明确边界,避免两种写法继续并存漂移 |
| **[L-93]** `src/components/ui/Pulse/PulseDot.tsx:10-13(↔ usePulse.ts:18-21)` | 重复 | Low | 高(批次双层审查验真) | PulseDot 重声明 `to = 1、duration = 700、delay = 0`,与 usePulse 默认值逐字相同(唯一有意差异是 from 0.5 vs 0.6):hook 默认节奏日后调整时 Pulse 包装器自动跟随、PulseDot 因钉死而静默漂移,pulse 家族节奏分叉无信号。 | 只显式覆盖 from、其余透传:`const { size = 6, color, testID, ...opts } = props` 后 `usePulse({ from: 0.5, ...opts })` |
| **[L-94]** `src/components/ui/Thumbnail/Thumbnail.tsx:45 ↔ styles.ts:14、16` | 重复 | Low | 高(批次双层审查验真) | ring 外圈圆角的 `+ 3` 魔数 = styles.ts 的 borderWidth 2 + padding 1,公式只活在注释(Thumbnail.tsx:42)而数值分居两文件:改 ring 厚度/留隙时漏改 `+3` 即外圈圆角与图不贴合(视觉缺口),与 [M-25](Switch THUMB_OFF_X)同类(派生常量未与来源同源)。图片加载失败路径无单独状态:bg 占位 `c.surfaceContainer` 同时兜底加载中/失败(styles.ts:5-7 有注释声明),属已记录的刻意设计,非缺陷。 | styles.ts 导出 `RING_BORDER` / `RING_PAD` 常量,组件侧改 `dim.borderRadius + RING_BORDER + RING_PAD` |
| **[L-95]** `src/components/ui/{BlurLayer,Spinner,Switch,Toast} 的 *.tsx ↔ *.web.tsx 平台文件对(正例对照:Pulse/usePulse.ts + usePulse.web.ts)` | 重复 | Low | 高(批次双层审查验真) | 平台分叉粒度不统一,证据显示 hook 级分叉显著优于组件级:Pulse 把分叉收窄到动画驱动 hook,渲染件 Pulse/PulseDot/Skeleton 单源、jscpd 零克隆;组件级分叉的 BlurLayer/Spinner/Switch/ToastHost 贡献了全部 5 对平台兄弟克隆(§3.1 #2/#5/#6/#7/#8)。Spinner 在已记克隆区之外,几何样式块(Spinner.tsx:52-59 ↔ web :72-79,六字段逐字一致)与 a11y 隐藏属性对(:64-65 ↔ :83-84)亦两端重复(jscpd 因 token 不足未报)。CLAUDE.md 只约定「`*.web.tsx` 兄弟文件」机制存在,未约定分叉应取的粒度。(位置已按移行指令改挂重复体文件对,正例 usePulse 留作对照。) | 把「分叉收窄到平台差异本体(hook / 驱动层),渲染保持单源」沉淀为 CLAUDE.md 约定;Spinner 按 [M-24] 重构时即可落地(useSpinAnimation.ts/.web.ts + 单一 Spinner.tsx) |
| **[L-96]** `src/components/ui/Input/types.ts:12-31 ↔ Textarea/types.ts:10-28(对照 TextField/types.ts:6-27)` | 重复 | Low | 高(批次双层审查验真) | 两份公开 types 各自重述 base 的共享 props(leading/trailing/error/containerStyle/disabled,JSDoc 近逐字)而非从 TextFieldBaseProps 派生,Omit 三元组('style'/'multiline'/'numberOfLines')亦两处复写;语义边界已现漂移 —— base 注明 error 空串不进 error 态(truthy 判定,TextField/types.ts:20-22),两份公开版均未同步,base 改语义时三文件手工对齐 | 公开 types 改组合派生(在 Omit<TextFieldBaseProps, …> 基础上收窄),JSDoc 单点维护;至少把 error 空串边界同步进公开版 |
| **[L-97]** `src/components/ui/Toast/toast.ts:8-23 ↔ src/components/ui/Confirm/confirm.ts:14-53(Step 4 跨族结论)` | 重复 | Low | 高(批次双层审查验真) | createImperativeHost 抽取评估(结论性记录):两族 API 模块真重复仅「`_id` 自增 + `_subs` Set + forEach 发射」≈4 行/族,Host 侧订阅 effect ≈6 行/处共 3 处(Toast 双端两处已由 [M-26] 覆盖,Confirm 为第 3 处同构);但语义核心彻底分叉 —— toast = 无状态事件推送(fire-and-forget、可连发、Host 内合并替换),confirm = Promise 单例锁(`_activeEntry` 互斥、resolve 闭包注入 entry、重入拒绝),通用工厂须参数化 entry 构造/并发策略/返回形态,抽象体量大于被消除的样板,且 2 族样本不满足 rule-of-three。结论:不值得抽 createImperativeHost,维持现状;若第 3 个命令式浮层(sheet()/alert() 类)落地则触发重评(注:本仓现无 BottomSheet 组件,CLAUDE.md 组件列举中的 BottomSheet 系陈旧引用——见 [L-72])。 | 维持现状;若修复期采纳 [M-26] 的 useToastEntry 重构,可顺带共享最小 `createSubscription<T>()`(subs + subscribe + emit,≈10 行收益)供两族 API 模块复用 —— 可选项,不强制 |
| **[L-98]** `src/components/business/Decorations/GradientWash.tsx:43 ↔ RadialHalo.tsx:39 ↔ AvatarWithRing/AvatarWithRing.tsx:28` | 重复 | Low | 高(批次双层审查验真) | useId 消毒一行三处复制(rule-of-three 已满):`autoId.replace(/[^a-zA-Z0-9_-]/g, '')` 仅前缀不同(gw- / rh- / av-),而「React 19 useId 含冒号、非合法 SVG id 字符」的 why 注释只在 GradientWash(:20-21)有 —— 另两处读者无从知道 replace 在防什么;第 4 个 SVG 组件出现时大概率再抄一份。 | 抽共享 `useSvgId(prefix)` 小 hook(Decorations 内 shared 或 utils),why 注释随 helper 收口 |
| **[L-99]** `src/theme/index.ts:9` | 解耦 | Low | 高(批次双层审查验真) | 裸 `ThemeContext` 对外导出但零文档(website docs 与 CLAUDE.md 的 ./theme 清单均未列):消费方可自挂 inline-value Provider 绕开 `ThemeProvider` 的 useMemo 契约,打穿 useThemedStyles 缓存;子树强制主题的正当需求已有嵌套 `<ThemeProvider forceScheme>` 覆盖,该导出无已知正当用途。 | 注释/文档标明「内部用途,自挂 Provider 须保证 value 引用稳定」,或评估下个 breaking 版本收回导出 |
| **[L-100]** `src/components/ui/Segmented/types.ts:1(↔ Tabs/types.ts:1-8)` | 解耦 | Low | 高(批次双层审查验真) | Segmented 从兄弟组件 `../Tabs` import TabItem 类型(头注释自言「共用 item 形状;命名独立以表达控件意图差异」)—— 本批核查范围内唯一的组件间横向**类型**依赖,Tabs 目录删改、迁移将连带 Segmented 编译;其余横向 import(Cell、Grid、TabBar、EntryCard、Empty → Icon,NavBar → IconButton)均为「组件组合组件」的正当方向。与 [M-22] tab-item primitive 决策强耦合:若抽 primitive,共享类型应随之上提。 | TabItem 上提共享位置(或随 primitive 落地),两组件各自 re-export 保持 API 不变;若 [M-22] 判豁免则此行随之豁免(共享类型即当前最小共享面) |
| **[L-101]** `src/components/ui/Confirm/types.ts:21-22(↔ confirm.ts:51、ConfirmHost.tsx:23)` | 解耦 | Low | 高(批次双层审查验真) | Subscriber 签名 `(entry: ConfirmEntry 或 null) => void` 的 null 分支是死协议:types.ts:21 注释承诺「收到 null 表示关闭」,但全仓唯一发射点 confirm.ts:51 只发非 null,关闭全靠 Host 本地 setEntry(null) 自治;Toast 的同名 Subscriber 即为非空签名(Toast/types.ts:39)。按类型实现第二实现/测试替身会为不存在的协议分支写处理逻辑。 | 删 `null` 分支对齐 Toast 签名;或反向把「程序化关闭(如 dismissActiveConfirm())」补成真实发射 null 的 API —— 二选一,类型与协议对齐 |
| **[L-102]** `src/components/business/Decorations/index.ts:4-10 ↔ src/components/business/index.ts:7-13` | 解耦 | Low | 高(批次双层审查验真) | barrel 漏导出公共 API 的构成类型:`GradientStop`(types.ts:13-17,已 export)是 GradientWashProps 自定义模式 stops(types.ts:36)与 ScreenBackdropProps.stops.light/dark(types.ts:93-96)两处公共 prop 的元素类型,但 Decorations 与 business 两级 barrel 均未 re-export → 包外无法具名引用,消费者写类型化 stops 构造须 `NonNullable<GradientWashProps['stops']>[number]` 间接表达(判别联合两臂均声明 stops——一臂 `?: never`——索引访问合法,tsc 实证可编译;绕行可用但不直观);字面量内联因结构化类型仍可用,故从低。对照:TextInputRef 不导出经核不构成同类缺口(纯 `ComponentRef<typeof TextInput>` 别名,消费者可从 react-native 自行表达,且 TextFieldBase.tsx:12 头注释明示 internal)。 | 两级 barrel 补 `export type { GradientStop }` |
| **[L-103]** `src/components/business/GlassStats/GlassStats.tsx:3` | 解耦 | Low | 高(批次双层审查验真) | 全仓唯一走层级 barrel 的跨组件 import:`import { BlurLayer } from '../../ui'` —— src/components 内其余 31 处跨组件引用全部直连组件目录或文件(`'../Icon'`、`'../Button/ButtonBase'`、`'../TextField/TextFieldBase'` 等,全量 grep 坐实),唯此处把 36 个 ui 组件全量拉进 GlassStats 模块图(Metro 不摇树;现包入口单 barrel 掩盖实害),且 ui barrel 日后若反向引 business 即刻成环的最大接触面;约定漂移点。 | 改 `from '../../ui/BlurLayer'` 对齐全仓直连惯例 |
| **[L-104]** `src/components/business/VersionPill/VersionPill.tsx:20、24` | 解耦 | Low | 高(批次双层审查验真) | 文案硬编码焊死通用性:「版本 」中文前缀与「build 」英文前缀直接拼进 JSX,无 override prop —— 非中文产品或需要「v2.8.0」形态的消费者整组件不可用;对照同仓先例 Confirm 的 zh 默认 + confirmLabel/cancelLabel 可覆盖(Confirm/types.ts:7-10),VersionPill 缺同款逃生口。business「复合但仍保持通用」边界下,文案是最典型的消费者差异点。 | 补 `versionPrefix?` / `buildPrefix?`(默认现文案,不 breaking),或改为 label 全量可传 |

## 3. 度量数据附录

### 3.1 jscpd(重复度)

**实际命令** —— 计划命令(`--ignore` / `consoleFull`)在 `yarn dlx` 解析到的 jscpd 5.0.5(Rust 重写的 `cpd` CLI)已不存在,按 v5 等价旗标执行,并加 json reporter 便于精确核对:

```sh
yarn dlx jscpd src --ignore-pattern "**/data.ts" --min-tokens 50 --reporters console-full,json --output /tmp/jscpd-report
```

已确认 `src/icons/data.ts` 被排除(console 与 JSON 报告中均无任何 data.ts 条目)。

**汇总**(jscpd 5.0.5,src/ 共 128 文件 6548 行):

| Format | 文件数 | 总行数 | 总 token | 克隆数 | 重复行 | 重复 token |
| --- | --- | --- | --- | --- | --- | --- |
| tsx | 56 | 3302 | 17055 | 7 | 113(3.42%) | 582(3.41%) |
| typescript | 72 | 3246 | 13075 | 1 | 16(0.49%) | 83(0.63%) |
| **合计** | 128 | 6548 | 30130 | 8 | 129(1.97%) | 665(2.21%) |

**克隆对清单**(8 对;行数/token 为单笔克隆体量):

| # | 克隆对(file:line) | 行数 | token |
| --- | --- | --- | --- |
| 1 | `src/components/ui/Avatar/Avatar.tsx:20-33` ↔ `src/components/ui/DrawerHeader/DrawerHeader.tsx:17-30` | 14 | 64 |
| 2 | `src/components/ui/BlurLayer/BlurLayer.tsx:3-21` ↔ `src/components/ui/BlurLayer/BlurLayer.web.tsx:10-22` | 19 | 63 |
| 3 | `src/components/ui/Grid/styles.ts:72-88` ↔ `src/components/ui/TabBar/styles.ts:27-43` | 17 | 83 |
| 4 | `src/components/ui/Segmented/Segmented.tsx:27-40` ↔ `src/components/ui/Tabs/Tabs.tsx:22-35` | 14 | 103 |
| 5 | `src/components/ui/Spinner/Spinner.tsx:24-36` ↔ `src/components/ui/Spinner/Spinner.web.tsx:46-60` | 13 | 110 |
| 6 | `src/components/ui/Switch/Switch.tsx:11-38` ↔ `src/components/ui/Switch/Switch.web.tsx:3-35` | 28 | 97 |
| 7 | `src/components/ui/Switch/Switch.tsx:63-76` ↔ `src/components/ui/Switch/Switch.web.tsx:49-62` | 14 | 74 |
| 8 | `src/components/ui/Toast/ToastHost.tsx:11-28` ↔ `src/components/ui/Toast/ToastHost.web.tsx:3-23` | 18 | 71 |

**分流初判**(Task 14 二审定性 —— 结论已记 §4.1,豁免候选清单见 §4.2):

- **约定性同形候选:0 对** —— 预期会成簇的 `styles.ts` `sizingFor`/`paletteFor` 结构与 `types.ts` Props 模板,在 min-tokens 50 下均未被报成克隆(结构同形但 token 序列差异足够大)——「0 对」仅为机器度量结论,不代表仓库无约定性同形;§4 豁免清单仍由深读期人工比对供给。唯一涉及 `styles.ts` 的 #3 经核读是 `makeStyles` 内 `badge`/`badgeText` 样式块的逐字段重复(仅 `top` 偏移不同),不属于 sizingFor/paletteFor 约定结构,不入候选。
- **入台账:8 对 → §2.1 共 7 行**(#6/#7 同属 `Switch.tsx ↔ Switch.web.tsx` 同一文件对、同一成因,合并 1 行)。其中 5 对(#2、#5、#6、#7、#8)是 `*.tsx ↔ *.web.tsx` 平台兄弟文件之间的重复 —— 平台分叉本身是仓库约定(CLAUDE.md「`*.web.tsx` 兄弟文件」),但分叉内被克隆的逻辑/常量是否应抽公共模块,台账逐对给出证据,留 Task 14 权衡(已裁:逐对入 §2.2([M-23]–[M-26]),平台分叉豁免候选集中 §4.2)。

### 3.2 madge(循环依赖 / 孤儿)

**实际命令**(`yarn dlx` 解析到 madge 8.0.0;两查询按计划分两次运行;src 内全部相对路径 import、无 `@/` 别名,默认解析即可全量定位,未需 `--ts-config`):

```sh
yarn dlx madge --circular --extensions ts,tsx src
yarn dlx madge --orphans --extensions ts,tsx src
```

两次均 `Processed 195 files`,与 `find src -type f \( -name "*.ts" -o -name "*.tsx" \) | wc -l` = 195 一致 → 覆盖 src 全量 ts/tsx(含 §3.1 jscpd 排除的 `data.ts`;jscpd 的「128 文件」是其自身统计口径,与此无冲突)。

**循环依赖** — 原始输出:

```
✔ No circular dependency found!
```

0 环,无台账项。

**孤儿模块** — 原始输出(8 个,madge 输出为相对 `src/` 路径):

```
components/index.ts
components/ui/BlurLayer/BlurLayer.web.tsx
components/ui/Pulse/usePulse.web.ts
components/ui/Reveal/Reveal.web.tsx
components/ui/Spinner/Spinner.web.tsx
components/ui/Switch/Switch.web.tsx
components/ui/Toast/ToastHost.web.tsx
index.tsx
```

**逐个判读**(排除流程:① 根入口 / ② package.json 字段引用 / ③ example、website workspace 引用 / ④ 平台变体 / ⑤ jest、babel 配置引用;全过 8 项,误报 7 + 真孤儿 1):

| Orphan(相对 src/) | 判定 | 排除理由 |
| --- | --- | --- |
| `index.tsx` | 误报 | ① 根入口;② `package.json#exports["."]` 的 `source` 直指 `./src/index.tsx`、`main`/`types` 指向其 bob 构建产物,`tsconfig.paths` 亦把包名映射到 `./src/index` —— 包的对外入口,src 内无人 import 是设计使然 |
| `components/ui/BlurLayer/BlurLayer.web.tsx` | 误报 | ④ 平台变体:native 兄弟 `BlurLayer.tsx` 同目录存在,目录内 `index.ts` 以无后缀 `./BlurLayer` import,Metro / bob 按平台后缀解析,madge 不认 `.web` 后缀、固定解析到 native 版 → `*.web.*` 必然报孤儿 |
| `components/ui/Pulse/usePulse.web.ts` | 误报 | ④ 同上,native 兄弟 `usePulse.ts` 存在 |
| `components/ui/Reveal/Reveal.web.tsx` | 误报 | ④ 同上,native 兄弟 `Reveal.tsx` 存在 |
| `components/ui/Spinner/Spinner.web.tsx` | 误报 | ④ 同上,native 兄弟 `Spinner.tsx` 存在 |
| `components/ui/Switch/Switch.web.tsx` | 误报 | ④ 同上,native 兄弟 `Switch.tsx` 存在 |
| `components/ui/Toast/ToastHost.web.tsx` | 误报 | ④ 同上,native 兄弟 `ToastHost.tsx` 存在 |
| `components/index.ts` | **真孤儿** | ①–⑤ 逐项排除均不命中,证据见下 |

**真孤儿证据链 — `src/components/index.ts`**(整文件仅 2 行:`export * from './ui'; export * from './business';`):

- ① 非根入口(根入口是 `src/index.tsx`)。
- ② package.json 各字段不引用:`exports` 仅映射 `.`、`./package.json`、`./docs-home.css`,无 `./components` 子路径;`main`/`types` 指向根入口产物;bob `source: src` 是整目录构建配置、不构成对单文件的引用。
- ③ workspace 零引用:`grep -rn "components/index"` 全仓(仅排除 node_modules / lib / .yarn / .git / 本审查自身产物 docs/reviews、docs/plans)0 命中;example/src 对本包的 import 全部是 `from '@unif/react-native-design'` 包根形式(子路径 import grep 0 命中);example + website 内 `design/(src|components|lib)` 深路径 grep 0 命中。
- ④ 非平台变体(无 `.web` 后缀亦无对应变体)。
- ⑤ jest 配置(package.json#jest 仅 preset / testEnvironment / 路径忽略项)与 babel.config.js(仅两段 presets,无模块映射)均不引用。
- 旁证:src 内绕过它的原因是根 barrel `src/index.tsx:9-10` 直接 `export * from './components/ui'` + `'./components/business'`;git 史上该文件由 `b090723`(phase 2+3 迁移)引入后零改动,且 `git log --all -S "from './components'"` 对 `src/index.tsx` 全历史 0 命中 → 根入口从未经由它 re-export,自引入起即未被使用。

**度量边界**(与 §3.1「0 对」边界句同理,防止「0 环 + 1 孤儿」被外推成「解耦无问题」):(a)孤儿判定是**文件级**入边检查——可达文件内 export 级的未使用代码不在本结论范围,且本次审查没有任务覆盖该粒度(madge/jscpd 均不查,深读 checklist 亦未列专项);(b)**0 环 ≠ 分层方向合规**——`ui → business` 这类越层 import 只要不构成环,`--circular` 不会报,方向合规由 Task 13 Step 3 的 grep 专项核查。两点加强语:madge 8 默认跟随 `import type` 边(实测双 type-import 互引即报环),故「0 环」已含类型层;src 内 grep 动态 import/require 为 0(命中均为注释),静态图即全图。另一条现成推论供 Task 13 复用:孤儿清单不含任何组件级 `index.ts` → 不存在整组件目录从 barrel 体系脱链,「漏导出」核查可收窄为 barrel 内 named export 完整性。

→ 记入 §2.1:真孤儿 1 项(Medium,维度=解耦);环 0 项。

**Task 13 回填(分层方向与 barrel 核查结论,上方 (b) 预留专项的闭环)**:① ui→business、theme/utils→components、向上引 barrel 三组 grep 全零输出(实施 + spec 审查两次独立复跑)——分层方向全面合规;② barrel 核查:ui barrel 导出 36/37,唯一未导出目录 TextField 系刻意 internal(TextFieldBase.tsx:12 注释明示),ButtonBase/TextFieldBase 等内部 primitive 无泄漏,三个 barrel 零重复导出;③ 向上引用 barrel 仅 GlassStats 1 处(已入台账)。解耦维度「零违例」结论以此为依据,Task 15 概评直接引用。

### 3.3 eslint 复杂度热点

**实际命令**(与计划一致,eslint 9.39.4 的 `--rule` 旗标直通可用,未走 overlay 兜底;实际退出码 0 —— 0 error,warning 不计入退出码):

```sh
yarn eslint "src/**/*.{ts,tsx}" \
  --rule '{"complexity":["warn",10],"max-depth":["warn",4],"max-lines-per-function":["warn",{"max":100,"skipBlankLines":true,"skipComments":true}]}'
```

覆盖面经 `-f json` 二次运行复核:实际 lint **195 个文件**,与 §3.2 madge 的 src 全量 ts/tsx 计数一致(含 `src/icons/data.ts` —— `eslint.config.mjs` 的 ignores 只排 node_modules/lib/website,纯数据文件无函数体,三规则天然 0 命中)。输出无基线规则混入(json 输出中 ruleId 仅含三条注入规则;注:基线 `yarn lint` 退出码 0 只证 0 error,不蕴含 0 warning,故此处以 json 复核为准),全部 warning 均来自本次注入的目标规则。

**汇总** —— 共 **6 条 warning,全部来自 `complexity`**;`max-depth`(>4)与 `max-lines-per-function`(>100 有效行)均 **0 命中** —— 0 也是有效结论:src 内无超过 4 层的嵌套块、无超过 100 有效行的函数体,这两个维度无深读线索。

**热点清单**(每文件恰 1 条 warning,文件聚合即逐条列出;按实测复杂度降序 = 深读优先序):

| 文件 | 位置(函数) | 规则 | 实测/阈值 |
| --- | --- | --- | --- |
| `src/components/ui/Stepper/Stepper.tsx` | 18:8(`Stepper`) | complexity | 22 / 10 |
| `src/components/ui/Cell/Cell.tsx` | 34:8(`Cell`) | complexity | 20 / 10 |
| `src/components/ui/TextField/TextFieldBase.tsx` | 25:3(`TextFieldBase`) | complexity | 20 / 10 |
| `src/components/ui/Spinner/Spinner.tsx` | 16:8(`Spinner`) | complexity | 12 / 10 |
| `src/components/ui/Spinner/Spinner.web.tsx` | 35:8(`Spinner`) | complexity | 12 / 10 |
| `src/components/ui/Button/ButtonBase.tsx` | 73:28(匿名箭头函数 —— `Pressable` 的 `style` 回调,多重三元合成样式数组) | complexity | 11 / 10 |

**度量边界**:超阈值只是深读优先线索,**不是缺陷判据**(spec §4)—— RN 组件渲染函数靠条件分支表达 variant/状态本属常态,是否构成问题由深读期结合上下文判断,故本节不向 §2.1 台账记行。后续深读任务优先精读上表 6 文件,头部三个(22/20/20,双倍于阈值)优先级最高。两条交叉线索供深读复用:(a)`Spinner.tsx ↔ Spinner.web.tsx` 同值 12 —— 被标记函数体恰包含 §3.1 jscpd #5 的克隆区(两端逐行重复的入参防御分支),与该台账行「抽 `sanitizeSpinnerProps`」的建议同源,若采纳则两端复杂度同步回落;(b)`ButtonBase.tsx:73` 仅超 1,但位于 Button/IconButton 共享的核心 primitive 上,影响面大于单组件。

→ 记入 §2.1:0 行(线索随深读任务消化)。

## 4. 豁免说明

> Task 14 初稿(Task 15 终稿)。「豁免」= 经核实不计缺陷、或留待修复期一次性决断的事项;凡已入 §2.2 的行,此处只挂决断选项,不重复立项。

### 4.1 约定性同形:jscpd 候选 0 对(结论与边界)

§3.1 机器度量(min-tokens 50)未把 `sizingFor` / `paletteFor` / `types.ts` Props 模板等约定性结构报成克隆 —— **约定性同形候选 0 对**,本步二审仅需记录结论与边界:① 该「0」只是机器度量结论(克隆检测的 token 序列对结构同形不敏感),不代表仓库无约定性同形;深读期人工确认的 sizingFor/paletteFor 家族(Button、Tag、Avatar、Stepper、Thumbnail 等)系 CLAUDE.md 明文组件约定(「styles.ts 里的纯函数」),按 spec「约定性同形」豁免,不入发现列表(Tag 的全裸数值、兜底糊弄等具体偏差已各自入账 [L-76]/[L-20],豁免不及于偏差)。② 唯一涉及 styles.ts 的克隆 #3(badge 样式块)经核读**不属**该约定结构,已正常入账([M-21]),不豁免。

### 4.2 待修复期决断清单(台账预告的豁免候选)

复核期不代决;集中列出各行内预告的豁免选项,修复期一次性裁决:

| 候选 | 关联行 | 豁免路径 | 采纳豁免时仍须做 |
| --- | --- | --- | --- |
| BlurLayer 平台兄弟样板克隆 | [M-23] | **倾向豁免**(样板占比高、抽取收益低,行内已记) | 两端共享推导(blurType/tint 计算)再增长时抽 `shared.ts` |
| tab 骨架三实例(Segmented/Tabs/TabBar) | [M-22] | 备选豁免:原子独立性优先(按本报告定义不属约定性同形,豁免须显式记录理由) | pressed 0.7 抽共享常量与 ButtonBase 同源;[L-34]/[L-48] tablist 与 [L-100] 共享类型仍应落地 |
| Chip 自建按压 chrome | [M-27] | 备选豁免:selected-toggle 语义自成一族 | pressed 0.7 共享常量同上;与 [M-22] 同批裁决 |
| ToastHost 双端订阅 / 开场同构 | [M-26] | 备选豁免:平台分叉约定(CLAUDE.md `*.web.tsx` 机制) | 行为契约(定位 / 动画清单)双端对账 —— [H-6] 即分叉漂移的现实代价 |
| Switch 平台克隆 | [M-25] | 仅 (b) Pressable 外壳可随平台分叉约定豁免 | (a) 派生常量 THUMB_OFF_X/ON_X 收口进 styles.ts 无豁免争议,应做 |
| createImperativeHost 跨族抽取 | [L-97] | **已评估维持现状**(2 族不满足 rule-of-three,抽象体量大于被消除样板) | 第 3 个命令式浮层(sheet()/alert() 类)落地时重评 |

### 4.3 「ui 基础原子」横向依赖白名单(grep 实证)

src/components 内全部组件间横向 import(Task 14 复核重跑 grep),方向均为「复合 → 原子」,与 Task 13 分层结论一致;新增横向依赖应落在此白名单形态内(直连组件目录,不走层级 barrel):

- **Icon** ← Button、Cell(含 Leading/types)、Checkbox、Empty、EntryCard(含 types)、Grid(含 types)、IconButton(含 types)、NavBar(types 引 IconName)、PasswordInput、Search、StatusDot、TabBar(含 types)
- **Pulse**(usePulse / PulseDot)← Skeleton、StatusDot
- **ButtonBase / Button** ← IconButton(深路径 `../Button/ButtonBase`)、Confirm
- **TextFieldBase**(internal)← Input、Textarea、Search(仅 `TextInputRef` 类型)
- **Input** ← PasswordInput、Search
- **IconButton** ← NavBar
- 例外两笔已入账,不在白名单:Segmented → Tabs 的 `TabItem` 类型依赖([L-100],随 [M-22] 决策走);GlassStats → `../../ui` 层级 barrel([L-103],应改直连)

### 4.4 经核实属刻意设计(不计缺陷)

- **ThemeProvider 仅以 `scheme` 为依赖的 useMemo** —— CLAUDE.md 明文契约:稳定引用是 `useThemedStyles` 缓存生效的前提;复核确认无需扩依赖。
- **AvatarWithRing 非标 maker 签名** —— styles.ts 头注释完整记录破例理由(props 派生尺寸无法走 `useThemedStyles` 的 `[colors, shadow]` memo,调用点自带 `useMemo([size, ringColor, c, shadow.brandAvatar])` 精确缓存),并附「不用 borderWidth 实现 ring」的 Fabric 抗锯齿 why —— 符合仓库注释标准,豁免。
- **TextField 目录不出 barrel** —— TextFieldBase.tsx:12 注释明示 internal,公开 API 为 Input/Textarea(§3.2 Task 13 回填已核)。
- **Thumbnail 加载中 / 失败共用占位底色** —— styles.ts:5-7 注释已声明的刻意设计([L-94] 行内已记非缺陷)。
- **Carousel autoPlay 定时器全委托库** —— 库源核验 cleanup→pause + stopped 守卫、手势 pause/resume 竞态合格([L-85] 行内正面项)。
- **暗色 shadow 置零(floating/glassBar/brandAvatar 例外)与亮暗 alpha 故意不同** —— theme 层既定架构约定(CLAUDE.md + colors/shadow 注释);本次入账的只是注释缺口([L-3]/[L-4])与类型纪律([L-5]),约定本身不立项。
- **`fixed.*` 物理常量直用不二次 r()** —— NavBar/TabBar 双双合规([L-86] 行内正面项),约定执行良好。

## 5. 基线与前提

- 基线 commit:`9c05acf`(自 main `ff626af` / v0.8.2 拉出;其后 merge 进的 main 提交均为纯文档改动,`src/`、`scripts/` 与 ff626af 一致)
- yarn typecheck:通过(退出码 0,无报错)
- yarn test:通过(3 个 suite / 19 个用例全部通过)
- yarn lint:通过(退出码 0)
- data.ts 生成一致性:原始再生成 hash 不一致(HEAD `sha1:8b3275e9851e6e57debcffb1c450dd11689248ce` → 再生成 `sha1:4678ebadc98790ff6a0255d531efba8d1a201380`,`shasum` 即 SHA-1),但再生成产物经仓库 prettier 配置格式化后与 HEAD 字节一致 → 内容同步,差异纯格式(生成脚本输出未 prettier 化所致,详见 §2.2 [L-1]);已 `git checkout -- src/icons/data.ts` 还原,工作区恢复干净
